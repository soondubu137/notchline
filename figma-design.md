# Codex in Notch — V1 Figma 设计要求

| 字段 | 内容 |
| --- | --- |
| 文档状态 | 草案，待持续微调 |
| 版本 | 0.1 |
| 日期 | 2026-08-09 |
| 对应产品版本 | V1 MVP |
| 上游文档 | [PRD.md](./PRD.md) |

## 1. 文档目的

本文档定义 Codex in Notch V1 的 Figma UI/UX 原型要求，用于指导视觉探索、交互设计、内部评审、可用性验证和后续 SwiftUI/AppKit 开发交付。

本文档重点回答以下问题：

1. V1 需要在 Figma 中设计哪些界面和状态？
2. 各状态之间如何转换？
3. 哪些元素必须组件化？
4. 原型必须覆盖哪些主流程和边缘场景？
5. 什么条件代表 Figma V1 原型可以进入原生开发验证？

若本文档与 PRD 冲突，以 PRD 为准。尚未确定的视觉参数在本文档中标记为“待定”或“探索项”，在设计评审后更新。

## 2. 设计范围

V1 Figma 原型必须覆盖三类界面：

1. **刘海状态组件**：收起状态和悬停展开状态，是 V1 的核心体验。
2. **首次启动与集成设置**：帮助用户理解并启用 Codex 状态集成。
3. **设置界面**：管理登录时启动、动画、最近完成任务和显示器偏好。

Figma 用于验证视觉层级、状态表达、内容密度、交互流程和动画意图。以下原生 macOS 行为不能仅依赖 Figma 验证，后续必须使用 SwiftUI/AppKit 原型测试：

- 真实摄像头刘海和安全区域定位。
- 菜单栏自动隐藏与全屏模式。
- 窗口层级、鼠标命中区域和焦点行为。
- 多显示器、桌面空间和无刘海降级行为。

## 3. 设计原则

### 3.1 一眼可读

用户不需要展开面板，就能快速分辨空闲、运行中、等待处理、已完成、失败和连接断开。

### 3.2 低干扰

收起状态尽量不超出菜单栏和刘海周围区域。运行中的动画应平静、连续；只有需要用户处理的状态才使用更明显的提醒效果。

### 3.3 融入 macOS

整体视觉应接近原生 macOS 菜单栏、浮层和系统状态组件，而不是网页卡片或独立悬浮窗。优先使用 SF Pro、SF Symbols、系统语义颜色和材质感。

### 3.4 状态优先于装饰

动画、颜色和形变均服务于状态传达。V1 不追求宠物化、拟人化或复杂装饰效果。

### 3.5 不只依赖颜色

等待、失败、完成等状态必须同时通过图标、文字、动画节奏或轮廓变化表达，以支持色觉差异用户。

### 3.6 渐进披露

收起状态只显示最重要的汇总信息；悬停展开后再显示任务标题、项目、时长和等待原因。

## 4. Figma 文件结构

Figma 文件建议使用以下页面结构：

```text
00 — Cover & Notes
01 — Foundations
02 — Components
03 — Compact Notch States
04 — Expanded Task Panel
05 — Onboarding & Integration
06 — Settings
07 — Interaction Flows
08 — Edge Cases
09 — Prototype
99 — Archive
```

各页面要求：

- `00 — Cover & Notes`：版本、负责人、更新时间、评审结论和待定项。
- `01 — Foundations`：颜色、字体、间距、圆角、阴影、材质和动画参数。
- `02 — Components`：所有可复用组件及其属性和变体。
- `03 — Compact Notch States`：收起状态矩阵。
- `04 — Expanded Task Panel`：展开面板、任务数量和内容密度变化。
- `05 — Onboarding & Integration`：首次启动、集成说明、检查、成功、失败和修复流程。
- `06 — Settings`：V1 设置界面及控件状态。
- `07 — Interaction Flows`：主要任务流的连接图和说明。
- `08 — Edge Cases`：长文本、异常状态、多任务、无刘海和不同显示环境。
- `09 — Prototype`：用于演示和测试的可运行原型入口。
- `99 — Archive`：被淘汰但需要保留的探索方案。

## 5. 设计画板与显示环境

### 5.1 基准画板

- 使用 `1512 × 982` 作为内置刘海屏幕的基准逻辑画板。
- 使用 `1440 × 900` 作为无刘海外接显示器的降级方案画板。
- 所有核心状态至少在深色和浅色桌面背景上各验证一次。
- 画板必须包含菜单栏、摄像头刘海和足够的桌面内容，用于判断组件是否遮挡工作区域。

以上尺寸用于设计对比，不代表最终代码中的固定像素尺寸。原生实现必须根据实际屏幕安全区域计算位置。

### 5.2 刘海模型

- 摄像头刘海作为不可显示内容的黑色实体区域处理。
- 不允许把必要文字或状态信息放在物理刘海内部。
- 状态信息应位于刘海两侧、边缘或紧邻其下方。
- Figma 中应保留一层可切换的“安全区域与命中区域”标注，仅用于设计评审，不出现在最终原型中。

### 5.3 初始尺寸探索范围

以下为第一轮设计探索范围，并非最终决定：

| 元素 | 初始探索范围 |
| --- | --- |
| 收起组件总宽度 | 约 220–280 |
| 收起组件可见高度 | 约 28–36 |
| 展开面板宽度 | 约 360–420 |
| 展开面板最大高度 | 约 420–520 |
| 单行任务高度 | 约 52–68 |
| 面板圆角 | 约 14–20 |
| 面板内边距 | 约 12–16 |

第一轮至少提供两种密度方案：紧凑版和舒展版。

## 6. 视觉基础

### 6.1 字体

- 主要字体使用 SF Pro。
- 时间、计数或需要对齐的短数字可以探索 SF Mono。
- 任务标题不得小于常规 macOS 菜单与浮层中可舒适阅读的尺寸。
- 文字层级至少包含：面板标题、任务标题、辅助信息、状态标签和时间信息。

### 6.2 图标

- 优先使用 SF Symbols。
- 每种重要状态必须有可识别图标或轮廓差异。
- 图标保持简洁，不使用品牌化插画或拟人角色。

### 6.3 颜色

颜色使用语义命名，不在组件内部直接使用无语义的固定名称。

建议建立以下颜色变量：

```text
surface/notch
surface/panel
surface/panel-hover
text/primary
text/secondary
border/subtle
status/idle
status/running
status/waiting
status/completed
status/failed
status/cancelled
status/disconnected
```

初始状态方向：

- 空闲：中性灰。
- 运行中：蓝色或冷白色。
- 等待批准或输入：琥珀色。
- 已完成：绿色。
- 失败：红色。
- 已取消：中性灰蓝。
- 连接断开：弱化灰色配警告标记。

所有颜色必须在浅色和深色背景下验证对比度，并提供不依赖颜色的辅助表达。

### 6.4 材质与层级

- 收起状态以接近摄像头刘海的纯黑或近黑材质为主。
- 展开面板可以探索 macOS 浮层材质、轻度透明和背景模糊效果。
- 阴影应轻量，只用于区分面板和桌面背景。
- 避免网页式厚重卡片、明显描边和过度渐变。

### 6.5 间距系统

- 使用 4 点基础网格。
- 常用间距优先采用 4、8、12、16、20、24。
- 任务行内的图标、标题、状态和时间必须保持稳定对齐。

## 7. Figma 变量与组件属性

原型应建立能够表达以下逻辑的变量或组件属性；具体使用 Figma Variables、Modes 或 Component Properties，由设计实现时选择：

```text
globalStatus
taskCount
isExpanded
hasNotch
colorScheme
reduceMotion
integrationConnected
showRecentTasks
displayPreference
```

建议值：

```text
globalStatus:
  idle
  running
  waitingApproval
  waitingInput
  completed
  failed
  cancelled
  disconnected

taskCount:
  0
  1
  3
  10+

colorScheme:
  light
  dark

displayPreference:
  mainDisplay
  builtInDisplay
```

## 8. 必须组件化的元素

### 8.1 `Notch / Compact`

组件属性至少包含：

- 汇总状态。
- 活动任务数量。
- 是否显示次级结果徽标。
- 浅色或深色环境。
- 正常动态或减少动态。

### 8.2 `Notch / Expanded`

组件属性至少包含：

- 任务数量。
- 是否包含等待任务。
- 是否包含最近完成或失败任务。
- 是否需要滚动。
- 连接正常或断开。

### 8.3 `Status Indicator`

必须包含以下变体：

- 空闲。
- 运行中。
- 等待批准。
- 等待输入。
- 已完成。
- 失败。
- 已取消。
- 连接断开。

### 8.4 `Task Row`

组件属性至少包含：

- 任务状态。
- 有或无项目名称。
- 有或无等待原因。
- 标题正常或超长。
- 默认、悬停和按下状态。
- 活动任务或最近结束任务。

### 8.5 其他组件

- `Task Count Badge`
- `Secondary Result Badge`
- `Attention Reason`
- `Empty State`
- `Disconnected State`
- `Scrollable Task List`
- `Menu Bar Fallback`
- `Onboarding Step`
- `Integration Status`
- `Settings Row`
- `Toggle`
- `Display Preference Picker`

## 9. 收起状态要求

Figma 必须提供完整的收起状态矩阵：

| 状态 | 必须表达的信息 | 动画方向 |
| --- | --- | --- |
| 空闲 | Codex 当前没有活动任务 | 静态 |
| 单任务运行 | 一个任务正在执行 | 低频脉冲或流动 |
| 多任务运行 | 多个任务正在执行，并显示数量 | 低频脉冲加数量徽标 |
| 等待批准 | 至少一个任务需要权限决定 | 明显但不刺眼的琥珀色提醒 |
| 等待输入 | 至少一个任务需要用户回复 | 与批准状态相近，但图标或节奏不同 |
| 已完成 | 最近一个任务成功完成 | 一次短暂绿色确认动画 |
| 失败 | 最近一个任务执行失败 | 红色错误提示，避免持续快速闪烁 |
| 连接断开 | 当前状态不可可靠获取 | 弱化显示并带警告标记 |

必须探索以下两个方向并进行比较：

1. 状态指示器位于刘海左右两侧。
2. 状态指示器位于刘海下方轻微延伸区域。

最终选择需要考虑可读性、遮挡程度、视觉平衡和真实 macOS 实现难度。

## 10. 展开面板要求

### 10.1 面板顶部

面板顶部至少显示：

- 产品名称或简短标题。
- 活动任务数量。
- 当前整体状态。
- 设置入口。

如果空间不足，可以隐藏产品名称，但必须保留任务数量和设置入口。

### 10.2 任务行信息层级

任务行从高到低显示：

1. 任务标题。
2. 状态和已运行或等待时长。
3. 项目名称或工作目录名称。
4. 等待批准或输入时的简短原因。

任务行不得显示：

- 完整提示词。
- 完整命令。
- 模型推理。
- 文件差异或代码内容。
- 凭据或敏感路径。

### 10.3 排序

任务排序必须体现以下优先级：

1. 等待批准。
2. 等待输入。
3. 运行中。
4. 最近失败。
5. 最近完成。
6. 最近取消。

同一优先级内，最近更新的任务排在前面。

### 10.4 任务数量场景

至少设计以下面板状态：

- 无活动任务。
- 一个运行中任务。
- 三个混合状态任务。
- 一个等待任务加多个运行中任务。
- 十个以上任务，需要滚动。
- 只有最近完成或失败任务。
- 连接断开，任务状态不可确认。

## 11. 首次启动与集成设置

首次启动流程至少包含以下画面：

1. 欢迎与产品价值说明。
2. 本地数据与隐私说明。
3. Codex 安装和主目录检查。
4. 请求用户确认安装生命周期钩子。
5. 等待用户在 Codex 中信任钩子的说明。
6. 集成成功。
7. 集成失败或部分可用。
8. 修复和重新检测入口。

要求：

- 不使用模糊的“授予所有权限”描述。
- 明确说明应用只读取任务状态元数据。
- 明确说明应用不能替用户批准 Codex 权限请求。
- 集成失败时提供下一步，而不是只显示错误代码。
- 允许用户暂时跳过设置，并进入连接断开状态。

## 12. 设置界面

V1 设置界面必须包含：

- 登录时启动。
- 启用或禁用状态动画。
- 显示或隐藏最近完成任务。
- 显示器偏好：跟随主显示器或优先使用内置显示器。
- Codex 集成状态。
- 重新检测或修复集成。
- 关于和版本信息。
- 退出应用。

设置界面可以探索以下两种形式：

1. 展开面板内的二级设置视图。
2. 独立的小型 macOS 设置窗口。

V1 原型需要选定一种主方案，并保留另一种方案作为对比。

## 13. 关键交互要求

### 13.1 悬停展开与收起

第一轮原型使用以下初始参数：

- 鼠标进入后约 `150 ms` 开始展开。
- 展开动画约 `180–220 ms`。
- 鼠标离开后约 `250 ms` 开始收起。
- 鼠标从收起区域移动到展开面板时，不得意外收起。
- 按 Escape 立即收起。

以上参数均为可调探索项，需在真实原生原型中复测。

### 13.2 展开动画

- 面板从刘海区域自然向下展开。
- 优先探索宽度和高度同时变化的形变，而不是突然出现独立卡片。
- 文字内容可以略晚于面板轮廓淡入，以降低视觉噪声。
- 收起时执行相反顺序。
- 减少动态模式下使用短暂淡入淡出，不使用明显缩放或弹簧动画。

### 13.3 任务行交互

- 悬停任务行时显示轻量背景反馈。
- 点击任务行模拟打开对应 Codex 任务。
- 原型中使用过渡页或注释表示“切换到 Codex”，不设计任务编辑行为。
- 等待批准的任务行可以更突出，但不得提供“批准”按钮。

### 13.4 状态转换

原型必须演示以下完整状态链：

```text
空闲
→ 运行中
→ 等待批准
→ 运行中
→ 已完成
→ 空闲
```

同时演示以下异常状态链：

```text
运行中
→ 连接断开
→ 重新连接
→ 运行中或失败
```

## 14. 必须制作的原型流程

### 流程 A：首次设置

```text
欢迎
→ 隐私说明
→ 检测 Codex
→ 确认安装集成
→ 等待信任
→ 设置成功
→ 空闲状态
```

### 流程 B：单任务正常完成

```text
空闲
→ 任务开始
→ 收起状态显示运行中
→ 悬停查看任务
→ 任务完成
→ 短暂绿色确认
→ 空闲
```

### 流程 C：等待人工处理

```text
运行中
→ 等待批准或输入
→ 收起状态提升提醒级别
→ 展开查看等待原因
→ 点击任务
→ 模拟打开 Codex
```

### 流程 D：多任务

```text
三个任务同时活动
→ 收起状态显示数量
→ 展开面板
→ 等待任务置顶
→ 运行中任务随后
→ 最近完成任务位于下方
```

### 流程 E：集成断开与修复

```text
连接断开
→ 展开查看说明
→ 进入设置
→ 重新检测
→ 恢复连接
```

### 流程 F：无刘海显示器

```text
切换至无刘海显示器
→ 使用标准菜单栏图标
→ 点击或悬停打开相同任务面板
```

## 15. 动画与微交互

V1 需要定义动画意图，但不要求在 Figma 中完全复现最终 SwiftUI 动画曲线。

必须提供：

- 运行中脉冲动画。
- 等待状态提醒动画。
- 完成确认动画。
- 失败提示动画。
- 收起到展开的面板形变。
- 任务行悬停反馈。
- 数量变化反馈。
- 减少动态版本。

动画原则：

- 运行中动画可持续，但必须安静。
- 等待动画可以更明显，但不能快速闪烁。
- 完成动画只播放一次。
- 失败状态可以保留，但错误动画只播放一次。
- 避免使用弹跳幅度过大的娱乐化动效。

## 16. 内容与示例数据

原型使用虚构任务数据，不使用真实用户提示词或项目内容。

建议示例：

| 任务标题 | 项目 | 状态 | 时间或说明 |
| --- | --- | --- | --- |
| 实现 Codex 状态事件适配器 | codex-in-notch | 运行中 | 2 分 14 秒 |
| 验证 macOS 刘海窗口定位 | codex-in-notch | 等待批准 | 需要允许运行本地测试 |
| 修复任务列表滚动问题 | codex-in-notch | 等待输入 | 需要选择默认显示器策略 |
| 更新产品需求文档 | codex-in-notch | 已完成 | 12 秒前 |
| 运行多显示器集成测试 | codex-in-notch | 失败 | 测试进程异常退出 |

文案要求：

- 任务标题优先使用动词开头，便于快速扫描。
- 等待原因保持一行，避免展示完整命令或敏感内容。
- 时间使用简短格式，例如“2 分 14 秒”“12 秒前”。
- 缺失标题时使用“未命名 Codex 任务”。
- 缺失项目名时隐藏项目字段，不显示空占位符。

## 17. 边缘场景

Figma 必须覆盖以下场景：

- 超长任务标题。
- 超长项目名称。
- 缺失任务标题。
- 缺失工作目录。
- 一个、三个和十个以上任务。
- 多个任务同时等待处理。
- 任务状态快速连续变化。
- 应用刚启动但状态仍在恢复。
- Codex 未安装。
- Codex 集成被禁用或未信任。
- 连接断开后存在过期任务。
- 浅色桌面和深色桌面。
- 高对比背景。
- 减少动态效果。
- 内置刘海屏幕。
- 无刘海外接显示器。
- 菜单栏自动隐藏。
- 全屏应用。

## 18. 无障碍要求

- 每个状态同时具有颜色以外的图标、轮廓或文字差异。
- 正文和辅助文字与面板背景保持足够对比度。
- 任务行命中区域不得过小。
- 原型注释中为核心组件提供建议的旁白标签。
- 等待批准和等待输入必须能够被文字区分。
- 减少动态版本不依赖动画传达状态。
- 设计时检查红绿色觉差异下的完成与失败状态。

建议旁白标签示例：

```text
Codex，三个活动任务，其中一个等待批准
验证 macOS 刘海窗口定位，等待批准，已等待四十五秒
更新产品需求文档，已完成，十二秒前
```

## 19. 原型验收标准

满足以下条件时，Figma V1 原型可进入原生开发验证：

1. 已完成空闲、运行、等待批准、等待输入、完成、失败、取消和连接断开八种收起状态。
2. 已完成一个、三个和十个以上任务的展开面板。
3. 已完成首次设置、单任务、多任务、等待处理、断开修复和无刘海六条原型流程。
4. 收起状态能够在不展开面板的情况下区分“运行中”和“需要处理”。
5. 展开面板能够清楚显示任务标题、状态、项目和时长。
6. 等待任务会按照产品优先级置顶。
7. 已提供浅色和深色环境验证。
8. 已提供正常动画和减少动态版本。
9. 已设计无刘海菜单栏降级方案。
10. 点击任务只模拟打开 Codex，不提供批准或编辑操作。
11. 所有核心元素均由可复用组件和语义变量构成。
12. 核心尺寸、间距、颜色、字体和动画意图均有开发标注。
13. 所有待定项均记录在设计文件封面页或本文档中。

## 20. 第一轮设计探索任务

第一轮不直接追求最终高保真方案，而是完成以下探索：

1. 为收起状态制作至少三种视觉方向：
   - 刘海左右状态点。
   - 刘海下方短条。
   - 与刘海轮廓融合的轻微下延形态。
2. 为展开面板制作紧凑版和舒展版两种信息密度。
3. 比较“悬停即展开”和“悬停预览、点击固定展开”两种交互。
4. 比较设置位于面板内部和独立设置窗口两种方案。
5. 选择一个主方向后，再补齐全部状态和交互流程。

## 21. 当前待定项

以下问题需要通过 Figma 探索和后续原生验证确定：

1. 状态信息主要位于刘海左右还是刘海下方？
2. 收起状态是否始终显示活动任务数量？
3. 等待批准和等待输入使用不同颜色，还是使用相同颜色配不同图标？
4. 展开面板采用多大宽度和任务行密度？
5. 鼠标离开后是否立即收起，还是允许短暂延迟？
6. 是否需要点击后将展开面板固定在屏幕上？
7. 最近完成和失败任务显示多少条、保留多久？
8. 设置入口位于展开面板还是标准菜单栏菜单？
9. V1 默认跟随主显示器，还是优先固定在内置刘海屏幕？
10. 全屏模式下是保持显示、仅在需要处理时显示，还是完全隐藏？

## 22. 设计参考资源

- [产品需求文档](./PRD.md)
- [Apple Design Resources](https://developer.apple.com/design/resources/)
- [Apple Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- [SF Symbols](https://developer.apple.com/sf-symbols/)

这些资源用于校准 macOS 视觉和交互习惯。Codex in Notch 的具体刘海形态仍需根据产品目标独立探索，并通过真实 SwiftUI/AppKit 原型验证。
