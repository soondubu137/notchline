# 未受官方公开支持的集成依赖清单

本文只记录 Codex in Notch 中依赖未被官方公开文档或公开 schema 支持的实现细节的生产 feature，按产品分表。目的不是列出所有没有使用 App Server 的功能，而是在 Codex Desktop 更新后出现兼容性问题时，能够快速定位真正的私有依赖。

当前验证基线：Codex Desktop `26.810.50856`（build `6644`），内置 Codex CLI `0.148.0-alpha.9`，验证日期 `2026-08-14`；审批归属与子智能体那两行单独验证于 Codex Desktop `26.818.41509`、内置 Codex CLI `0.149.0-alpha.4.1`，验证日期 `2026-08-22`；Claude Desktop `1.30096.5`，Claude Code CLI `2.1.233`，验证日期 `2026-08-16`；Claude Code 子智能体那一行单独验证于 Claude Code CLI `2.1.241`，验证日期 `2026-08-23`；两张表里「子智能体停在对话框上时行会说」那一行单独验证于 Codex CLI `0.149.0-alpha.4.1`（隔离 `CODEX_HOME`）与 Claude Code CLI `2.1.241`（pty 交互式会话），验证日期同为 `2026-08-23`。

“官方公开支持”包括官方文档和公开 schema 中定义的 App Server、Codex Hooks、CLI/SDK 接口、Desktop deep link，以及实现中使用的 macOS 公共 API。仅仅没有通过 App Server 实现，不构成登记理由。官方 [Codex Hooks](https://learn.chatgpt.com/docs/hooks) 明确公开 lifecycle 事件、`hooks.json` 配置位置和信任流程；官方 [Commands](https://learn.chatgpt.com/docs/reference/commands#deep-links) 公开 Desktop deep link，这些能力不列入本表。

**一条明确不登记的观察，写在这里以免以后重新推导。** 2026-08-20 实测 `config.toml` 中信任记录的 key 形状为 `<hooks.json 路径>:<snake_case 事件名>:<group 下标>:<handler 下标>`，第三、四段都是数组下标。这属于私有细节，但**没有任何 feature 读它**：本应用的两条合并规则（只在尾部追加、已经正确的安装不写文件）在这条观察成立与否时都正确，只是知道它之后能说清楚它们为什么是对用户自己的定义有承载作用的（见 [ADR 0014](adr/0014-the-codex-hook-definition-is-never-rewritten.md)）。按 `AGENTS.md` §8.1「不因为没走 App Server 就登记」，也按同一条的「必须是 feature 依赖了私有细节」，这里没有可登记的行。真要精确报告每条定义的信任状态就得去读这个 key，那是一个 ADR 级别的决定，并且会在本表新增一行。

## Codex Feature 清单

| Feature | 这个 feature 是什么 | 为什么官方公开支持的接口无法实现 | 实现方法 | 依赖级别与失效信号 | 代码定位 |
| --- | --- | --- | --- | --- | --- |
| Desktop Project / `Chats` 身份 | 展开列表为每个 thread 显示 Codex Desktop 侧边栏中的真实 Project 名称；只有 Desktop 明确标记为无 Project 的 thread 才显示 `Chats`。 | 当前公开 App Server Thread schema、Hooks 和 Desktop deep link 都不提供 Desktop `projectId`、`projectName` 或 thread 到 Project 的成员关系。`thread.section` 是独立的 Thread Section，不是 Desktop Project。 | 只读 `$CODEX_HOME/.codex-global-state.json`；可用 `NOTCHLINE_CODEX_HOME` 显式覆盖状态根目录。用 `thread-project-assignments[threadId]` 取得 assignment；`local` assignment 连接 `local-projects[projectId].name`，`remote` assignment 连接 `remote-projects[id].label`；只有 `projectless-thread-ids` 明确包含 thread ID 时返回 `Chats`。**当前 schema 的最小 key 集合不是四个顶层 key**：`thread-project-assignments` 与 `projectless-thread-ids` 至少存在其一，且只要定义了任何 Project 就必须存在 `thread-project-assignments`。`remote-projects` 只在存在云端 Project 时才被写入，实测健康状态文件中整个 key 缺失，因此不能要求；`local-projects` 同理。整份当前快照要么全部成立要么全部作废：任一 assignment 的 thread id 为空、`projectKind` 不是 `local`/`remote`、或 `projectId` 指向未定义的 Project，都直接拒绝当前文件并降级到 `.bak` / last-known-good，绝不跳过该条 assignment。主文件失败时读取 `.bak`，两者失败时保留 last-known-good。拒绝 symlink、非当前用户普通文件、超过 4 MiB 的文件、冲突成员关系及不兼容 schema。 | **Desktop 私有只读 schema。** 高版本风险。典型信号：Project 全部显示 `Project unavailable`，并且**一定伴随诊断**——schema 不兼容、缺少 thread assignment、未支持的 Project kind、assignment 指向未定义的 Project，或空 thread id；对应单测失败。因为部分或未知的映射不再算成功，key 改名会立刻在诊断里现形，而不是安静地少映射几个 thread。Desktop 更新后首先检查四个顶层 key 的名称、assignment 的 `projectKind/projectId` 以及本地 `name`、远程 `label`。 | [`CodexDesktopProjectMetadata.swift`](../Notchline/Notchline/CodexDesktopProjectMetadata.swift)、[`LiveCodexMonitorService.swift`](../Notchline/Notchline/LiveCodexMonitorService.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| Desktop 已读后自动移除 Completed 会话 | Completed 会话保持在列表中，直到 Codex Desktop 的未读蓝点消失；用户直接在 Desktop 打开并读过会话后，Notch 自动隐藏该行。Running、Input 与 Approval 会话不受未读状态影响。 | 当前公开 App Server Thread schema、Hooks 和 deep link 都没有 Desktop `hasUnreadTurn` 快照或已读变化通知；`thread/read` 只是读取 Thread 内容，不能表达蓝点语义。 | 只读 `$CODEX_HOME/.codex-global-state.json` 中的 `electron-persisted-atom-state.unread-thread-ids-by-host-v1.local`。用 `O_EVTONLY` + 目录级 `DispatchSourceFileSystemObject` 监听原子替换，250 ms trailing debounce 后刷新；现有 1 秒监视轮询负责 watcher 失效兜底。主文件失败时读取 `.bak`，再退到 last-known-good，但**只有当前主文件成功解析的 generation 才能隐藏新会话**。Completed 首次出现时保留 2 秒以覆盖 Desktop 约 500 ms 的持久化延迟；一旦观察过未读后再确认消失则立即隐藏。拒绝 symlink、非当前用户普通文件、超过 4 MiB、空/重复 id 及不兼容 schema；不连接私有 IPC、不写 Desktop 状态。 | **Desktop 私有只读 schema。** 高版本风险。典型信号：用户在 Desktop 阅读后 Completed 行持续保留；诊断包含“Desktop 未读状态 schema 不兼容”或“无法读取 Codex Desktop 未读状态”；目录原子替换测试失败。解析、权限或版本错误时 fail closed：保留尚未隐藏的 Completed 行，且不把空集合当成有效已读证据。Desktop 更新后首先检查 `electron-persisted-atom-state` 与 `unread-thread-ids-by-host-v1.local`、500 ms 持久化节流和主文件/`.bak` 原子替换行为。 | [`CodexDesktopUnreadState.swift`](../Notchline/Notchline/CodexDesktopUnreadState.swift)、[`LiveCodexMonitorService.swift`](../Notchline/Notchline/LiveCodexMonitorService.swift)、[`MonitorStore.swift`](../Notchline/Notchline/MonitorStore.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| 自动审查的 thread 不再显示 Approval needed | Codex Desktop 的「Approval for me」（`guardian-approvals` 代理模式，CLI 的 `--approve-for-me`）把审批交给自动审查者。这类 thread 的行在整个审查期间保持 Running，不再报一次 Approval needed 再自己变回去。审批会问到人的 thread 完全不受影响，`request_user_input` 在任何设置下都照常进入 Input needed。 | 官方 Hooks 的 `PermissionRequest` 只证明权限管线跑过，不证明有人被问：实测 2026-08-22（CLI `0.149.0-alpha.4.1`，非交互 `codex exec --approve-for-me`，现场没有任何人可问）它照样触发，配对的 `PostToolUse` 在 2.5 s 后到达，借用 id 的等待区间横跨的是「决定 + 执行」。官方 `permission-request.command.input` schema（随 CLI 二进制分发）里没有任何字段能区分两种情形——`permission_mode` 两边都是 `default`。App Server 侧同样没有：`thread/list` 与 `thread/read` 的 Thread payload 不含 `settings`／`approvalsReviewer`（实测同日），`config/read` 只返回 `config.toml` 里的值，而 Desktop 是**逐 thread**下发这个设置的，用户的 `config.toml` 里根本没有这个键。 | 只读 `$CODEX_HOME/.codex-global-state.json` 的 `electron-persisted-atom-state.heartbeat-thread-permissions-by-id.<threadId>.approvalsReviewer`（可用 `NOTCHLINE_CODEX_HOME` 覆盖状态根目录）。值恰好是 `auto_review` 时，`CodexSnapshotParser.session` 把该行的 `approvalNeeded` 降为 `running`；`inputNeeded`、`completed` 与 reducer 的其余一切都不变。**方向单向**：只有被证明为 `auto_review` 才降级。同一条目里的 `approvalPolicy` 有时是字符串、有时是带 `granular` 表的对象，因此**只解码 `approvalsReviewer` 一个字段**，不为一个没被问到的形状 fail。没有自己的 watcher——与未读集合同一个文件，那边的目录 watcher 已经会为它的每一次写入唤醒刷新。拒绝 symlink、非当前用户普通文件与超过 4 MiB 的文件；解析失败保留上一次成功的映射（Desktop 频繁原子重写这个文件，落在替换中间是常态，掉回「没有任何 thread 是自动的」会把闪烁放回来一拍），从来没成功过则是空集合。不写 Desktop 状态、不连接私有 IPC。 | **Desktop 私有只读 schema。** 中版本风险，且失效方向是安全的：key 改名、条目消失或出现本 build 不认识的 reviewer 值，都退回本次改动之前的行为（自动审查的 thread 重新出现短暂 Approval needed），不会反过来把真实审批藏起来。典型信号：用户报告审批状态的闪烁回来了。Desktop 更新后先检查 `heartbeat-thread-permissions-by-id` 是否仍存在、`approvalsReviewer` 的词表是否仍是 `user` / `auto_review`；实测 2026-08-22 该映射覆盖最近四十条 thread 里全部 `user` 线程，缺席的只有 `subagent` 线程，而那些从不成行。另一条可用来源是每份 rollout 的 `turn_context.approvals_reviewer`（那才是该轮真正使用的权威），被否决的理由是要在 JSONL 里向前扫到该轮的 `turn_context`，位置随该轮已产出的条目数任意后退；同一份文件里的 host 级 `permission-selection-by-host-id:local` 说的是**下一条** thread 会用什么，不是这条正在用什么。 | [`CodexDesktopApprovalRouting.swift`](../Notchline/Notchline/CodexDesktopApprovalRouting.swift)、[`LiveCodexMonitorService.swift`](../Notchline/Notchline/LiveCodexMonitorService.swift)；测试 `desktopApprovalRoutingNamesOnlyTheAutomaticallyReviewedThreads`、`desktopApprovalRoutingKeepsTheLastMapAndClaimsNothingWithoutOne`、`automaticallyReviewedThreadKeepsRunningWhileAnApprovalIsDecided`、`automaticallyReviewedThreadNeverPublishesApprovalNeeded` |
| 子智能体仍在跑时行尾说出来 | 一条 Thread 自己的 Turn 结束之后，如果它派生的子智能体还在跑，该行进入 Completed（预览是主智能体的最终回答），并在计时腾出来的那个位置写 `1 subagent` / `N subagents`；最后一个子智能体收尾后该位置清空。**同一个事实还改写行以外的四处**：收起态汇总与产品标记按 Running 画（尾翼写子智能体总数，没有轮次在计时时那里只有一个数字）、列表排序把该行排进 Running 那一档、终态未读成员关系门不再因为 Desktop 报告「已读」而把它隐藏，而最后一个子智能体收尾后的 settling 窗口从**那一刻**起算而不是从主智能体的 `Stop` 起算。手动移除仍然可用，且是计数卡死时唯一的人工出口。**这一行同时是一个缺陷修复**：在它之前，子智能体的第一条 `PreToolUse` 会被当作同一 Thread 的新轮次接管，主智能体的 `Stop` 随即被当作退休轮次的迟到事件丢弃，行永远停在 Running。 | 事件名 `SubagentStart` / `SubagentStop` 与字段 `agent_id` 都在随 CLI 二进制分发的官方 hook JSON schema 里（`subagent-start.command.input`、`subagent-stop.command.input`，以及 `pre-tool-use` 等四个 input schema 中的可选 `agent_id`），**这一半是公开的**。不公开的是**子智能体的 hook 用哪一个 `session_id` 盖章**：官方文档没有说过它是父 Thread 的。而本应用把 `session_id` 当作 Thread 主键，所以「子智能体的事件会落在父 Thread 的行上」这条前提必须靠实测确定，官方没有任何接口可以问。App Server 侧也答不了：`thread/list` 的子智能体 thread 带 `parentThreadId`／`agentRole`，从来不成行，而正在跑的子智能体数量不在任何 Thread payload 里。 | 实测 2026-08-22（Codex Desktop，内置 CLI `0.149.0-alpha.4.1`）：子智能体 rollout 的 `session_meta` 里 `session_id` 是**父 Thread**、`id` 才是它自己，`parent_thread_id` 另有其字段；`~/.codex/logs_2.sqlite` 中子智能体 `run_turn_stop_hooks` 的一条 WARN 直接写着 `failed to resolve parent transcript path for subagent hook parent_thread_id=…`。据此：注册 `SubagentStart` 与 `SubagentStop`（定义总数 5 → 7）；`HookPayload` 解 `agent_id`；reducer 里**带 `agent_id` 的其他事件一律消费后丢弃**（成对丢，否则失信探测误报），两条子智能体边界不走 `mutateExactTurn`（它们携带的是子智能体自己的 `turn_id`），而是按 `agent_id` 加减该 Thread 当前 Turn 上的 `runningSubagentIDs`，且不推进 `lastEventAt`。 | **一半公开 schema，一半实测行为。** 中版本风险，失效方向安全：若某次更新改成用子智能体自己的 `session_id` 盖章，那些事件会落到一条永远不成行的 thread 上——行照常在主智能体的 `Stop` 上进入 Completed，只是行尾不再出现 `1 subagent`、收起态也不再为它写 Running，**不会**回到停在 Running 的旧缺陷（那条缺陷的成因恰好是同一批事件落在父 Thread 上）。若 `agent_id` 改名或消失，退化成同一个样子。典型信号：派生过子智能体的行不再显示 `N subagents`。**只有一个方向是加重的，并且是明写接受的**：`SubagentStart` 到达而 `SubagentStop` 永久丢失时，此前只是一行文字发呆，现在**整个收起态会长期停在 Running、矩阵一直在动**，直到该 Thread 离开列表或用户右键移除该行。没有安全的自愈可用——计时器推断被 `AGENTS.md` §6.2 禁止，而「正在跑的子智能体数量」不在任何 Thread payload 里（列出并不等于在跑，`thread/list` 也答不了）。接受的理由与取舍写在 [`PRD.md`](PRD.md) §6.2，单独跟踪在 [#102](https://github.com/soondubu137/notchline/issues/102)。Desktop 更新后先 `strings -a /Applications/ChatGPT.app/Contents/Resources/codex` 查 `subagent-stop.command.input` 是否仍在、`agent_id` 是否仍是它的必填字段。 | [`HookIntegration.swift`](../Notchline/Notchline/HookIntegration.swift)、[`MonitorDomain.swift`](../Notchline/Notchline/MonitorDomain.swift)、[`MonitorStore.swift`](../Notchline/Notchline/MonitorStore.swift)、[`CodexDesktopUnreadState.swift`](../Notchline/Notchline/CodexDesktopUnreadState.swift)、[`LiveCodexMonitorService.swift`](../Notchline/Notchline/LiveCodexMonitorService.swift)、[`NotchOverlayView.swift`](../Notchline/Notchline/NotchOverlayView.swift)；测试 `aSubagentOutlivingItsTurnLeavesTheRowFinishedAndStillWorking`、`aRunningRowKeepsTheTimerAsItsOnlyMark`、`aSubagentBoundaryNeverOpensOrEndsATurn`、`aFinishedTurnWithASubagentStillRunningSummarisesAsRunning`、`aFinishedRowWithASubagentSortsWithTheRunningOnes`、`aReadThreadWithASubagentStillRunningKeepsItsRow`、`aSubagentBoundaryStampsItsOwnInstantAndNotTheTurns` |
| Codex Desktop 进程身份门槛与在场 | 两件事共用同一次查询：(1) 确认本次 Codex in Notch 启动后收到的实时 Hook 仍属于同一个 Codex Desktop 进程生命周期；(2) 回答 Codex 的**在场**——它是否处于打开状态，这是收起态 `Connected` / `Disconnected` 的一半（另一半是 availability）。它仍不参与判断 App Server 快照是否已确认，也不允许持久化 Hook 信任恢复 Ready。 | 官方 App Server 与 Hooks 不提供 Desktop 应用进程生命周期查询，也不提供「应用是否打开」的查询。实现依赖观察到的 Desktop bundle identifier `com.openai.codex`，该标识未作为 Codex 集成兼容契约公开。 | 用 macOS 公共 API `NSRunningApplication.runningApplications(withBundleIdentifier:)` 获取当前 Desktop PID；每次刷新取一次，两个用途共用该结果。只有本次启动 cutoff 后实际消费了合法 Hook，才把该观察绑定到当前 PID。**PID 绑住的是 Turn 本身，不只是要不要发布**：当前 PID 与担保 reducer 里那些 Turn 的那个不同，就在 drain 之前把它们整体退休（`HookEventRepository.discardTurns()`），查不到 PID 时还要在 drain 之后再退一次（将死进程派出的 helper 仍可能在路上）；之后重新走 App Server 当前快照或等待新 Hook。只退 Turn，不动「hook 触发过」这两个观察标志。此前只有观察标志被绑住，于是 Desktop 崩溃重开后第一条 hook 会把上一个进程的 Running 轮次连同它的计时一起复活，且再无任何证据能撤掉它（CR-Fable-007）。查到 PID 即在场为「已打开」，查不到即「未打开」；这是内核事实，因此 Codex 的在场永不为「未知」。App Server `initialize` 与 `thread/list` 的公开响应仍独立决定 availability，不受此私有门槛影响。 | **未公开的 Desktop bundle identifier。** 中版本风险，**且自在场制起后果变重**。典型信号：Desktop 更新 bundle id 后，启动后的 Hook 无法保持低延迟实时分支——这一条不变；新增的一条是收起态会在 Codex 明明开着时显示 `Disconnected`，因为在场恒为「未打开」。availability 仍会正常进入 Ready，因此展开面板与设置里看不出异常，只有收起态说谎。对应 PID 绑定测试与 `onlyAProductThatIsBothOpenAndReachableCountsAsConnected` 失败。 | [`LiveCodexMonitorService.swift`](../Notchline/Notchline/LiveCodexMonitorService.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| App Server 可执行文件发现与启动 | 找到与 Desktop/CLI 匹配的 `codex` 可执行文件，并启动只读 `app-server --listen stdio://` 连接。 | `app-server` CLI 子命令本身是官方公开能力，但官方契约不包含 Codex Desktop bundle 内的可执行文件安装位置。 | 优先使用 `NOTCHLINE_CODEX_PATH`；否则检查 `/Applications/ChatGPT.app/Contents/Resources/codex`、旧 `/Applications/Codex.app/...`，最后搜索 `PATH`。找到可执行文件后用 `Process` 启动公开的 `app-server --listen stdio://` 并完成 initialize/initialized 握手。 | **Desktop 私有打包路径。** 中版本风险。典型信号：`executableNotFound`、Desktop 更新后 bundle 内资源位置变化、进程启动失败。 | [`CodexAppServerClient.swift`](../Notchline/Notchline/CodexAppServerClient.swift) |
| 子智能体停在对话框上时行会说 | 一条 Thread 的某个子智能体正卡在审批对话框上时，该行行尾那一格（计时或计数）改用需要用户处理的亮色，收起态与排序按 Approval needed 处理该行。与该 Thread 自己的轮次是不是终态无关。被审查为 `auto_review` 的 thread 不进入这一档。 | 事件名与 `agent_id` 字段都在官方 schema 里（`permission-request.command.input` 的 `agent_id` 是可选字段），**这一半是公开的**。不公开的有两件：**子智能体的 `PermissionRequest` 用哪一个 `session_id` 盖章**（官方没说过是父 Thread 的，而本应用把它当 Thread 主键），以及**子智能体的审批交给谁**——`thread/list` 与任何 Thread payload 都不报这件事，Desktop 的 `heartbeat-thread-permissions-by-id` 里也**没有 subagent 线程的条目**。 | 实测 2026-08-23（隔离 `CODEX_HOME`，CLI `0.149.0-alpha.4.1`，`codex exec --approve-for-me` 派生一个子智能体去跑被沙箱挡住的联网命令）：`PreToolUse`(`agent_id`, `tool_use_id`) → 30 ms 后 `PermissionRequest`(`agent_id`，无 `tool_use_id`) → `PostToolUse`(同一对) → `SubagentStop`；`session_id` 是父 Thread，`agent_id` 是子智能体自己的 thread id，`turn_id` 是子智能体自己的轮次。据此：这些事件进入按 `agent_id` 索引的 `AgentWaitSlots`，借用**该子智能体**仍打开的调用，三条关闭规则加一条封顶（`tech-design.md` §9.2）。审批归属由**只读 rollout** 确定：扫本机 119 份 rollout，父子都在盘上的 72 份子智能体全部继承父 thread 当时的 `turn_context.approvals_reviewer`，零例外，两种取值都出现过——因此沿用行自己那道 `approvalsReachTheUser(threadID)` 闸门。 | **一半公开 schema，一半实测行为。** 中版本风险，失效方向安全：盖章方式改变或 `agent_id` 消失，这些事件落到一条永远不成行的 thread 上，行退回「只写 `N subagents`、不转亮」，不会产生错误状态。**加重的方向只有一个**：`SubagentStop` 永久丢失且那一格还留着等待时，收起态会长期停在 Approval needed 并亮着——与 [#102](https://github.com/soondubu137/notchline/issues/102) 是同一处，因为标志被封顶在计数里，两者一起清零，人工出口也仍是右键移除。另一条要盯的是继承关系：若某次更新让子智能体不再继承父 thread 的 `approvals_reviewer`，`auto_review` 的 thread 上会藏起一个真的对话框——Desktop 更新后按 `docs/technical-explorations/subagent-row-consistency/` §6.3 的方法重扫一次 rollout 即可复验。 | [`HookIntegration.swift`](../Notchline/Notchline/HookIntegration.swift)、[`MonitorDomain.swift`](../Notchline/Notchline/MonitorDomain.swift)、[`LiveCodexMonitorService.swift`](../Notchline/Notchline/LiveCodexMonitorService.swift)、[`NotchOverlayView.swift`](../Notchline/Notchline/NotchOverlayView.swift)；测试 `aSubagentsApprovalReachesTheRowItBelongsTo`、`aSubagentsApprovalNeverTouchesTheTurnsOwnSlots`、`aRefusedSubagentCallStopsSayingApprovalNeeded`、`aSubagentStopClearsWhateverThatAgentWasWaitingOn`、`anInternalAgentsCallIsNotASubagent` |

## Claude Code Feature 清单

| Feature | 这个 feature 是什么 | 为什么官方公开支持的接口无法实现 | 实现方法 | 依赖级别与失效信号 | 代码定位 |
| --- | --- | --- | --- | --- | --- |
| 会话标题 | 展开列表为每个 Claude Code 会话显示 Claude Code 自己在用的标题：用户设置的标题优先于自动生成的标题。 | 官方 Hook payload 只给出 `transcript_path`，不给标题；`claude agents --json` 只返回从目录派生的 `name`，那是 Project 不是标题；没有任何公开接口报告会话标题。 | 只读 `~/.claude/projects/<目录>/<sessionId>.jsonl`，解析 `custom-title` / `ai-title` 两种记录，取最后出现的一条，用户标题优先。目录名按「工作目录路径中的 `/` 全部替换为 `-`」推断；**该推断失败时退回按 `<sessionId>.jsonl` 文件名在 projects 下逐目录查找**，所以目录命名规则单独变化不会导致标题丢失。只读文件两端各 64 KiB，不整文件扫描（本机已见 16 MB 的 transcript）；按 `(size, mtime)` 缓存，会话消失即丢弃。解析失败、记录类型不认识、文件不存在一律返回无标题，行显示 `Untitled`，**绝不退回目录名**。预览开关关闭时同样显示 `Untitled`——标题也是用户内容。 | **Claude Code 私有只读 schema。** 中高版本风险。典型信号：所有 Claude Code 行显示 `Untitled`；标题解析单测失败。CLI 更新后先检查 `custom-title` / `ai-title` 记录类型与其 `customTitle` / `aiTitle` 字段，再检查 projects 目录命名规则。 | [`ClaudeCodeTranscriptReader.swift`](../Notchline/Notchline/ClaudeCodeTranscriptReader.swift)、[`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| 额度窗口读数 | 页脚为 Claude Code 画两条规则：5 小时会话窗口与 7 天全模型窗口。 | 命令本身是公开的，且**不产生任何模型请求**（实测 `total_cost_usd: 0`、`num_turns: 0`、`duration_api_ms: 0`、约 2 秒）。不公开的是它的输出形状——`result` 是写给人看的一段话，没有结构化字段。 | 运行 `claude -p "/usage" --output-format json`，只按两个整行前缀取值：`Current session:` 与 `Current week (all models):`。**绝不扫描"第一个百分号"**——同一段输出下方的自由文本里就有 `94% of your usage was at >150k context` 这类数字。百分比按"已用"上报，规则画 `100 - used`。重置时间不含年份，按当前年推断，落在过去则加一年；**整点的时间不写分钟**——午夜写作 `12am` 而不是 `12:00am`，因此分钟部分必须可选。**`--output-format json` 只承诺那个对象的形状，不承诺它独占 stdout**：实测 2.1.234 上五次里有两次会在对象之后再打一行 `Client.listTools() called but server does not advertise tools capability - returning empty list`，把整份 stdout 当一个文档解码会因为尾部多余文本而整体失败，所以逐行解码——但**取的是第一个真的是答案的信封，不是第一个解得开的**：信封的每个字段都可选（命令怎么结束决定它写下哪几个），合成出来的 `Decodable` 连 `{}` 都解得开，于是任何一行 JSON 都能冒充信封。真有一行抢在答案前面（某个 MCP server 的，或将来某版 `claude` 的），每一次读数都会被记成失败：没有 `result`，调用方按失败退避，过了 900 秒信任上限就把两条规则清空——而命令其实每次都答对了；也没有 `session_id`，那次读数留下的 transcript 反查不到目录，Settings 的磁盘占用那行永远停在 `Calculating…`（CR-Fable-040）。判据是 `result`，其次 `session_id`，带 `result` 的行优先。stderr 必须丢弃而不是接一根没人读的管道（写满 64 KB 后子进程会永远阻塞）。第三个窗口（某个具名模型的周上限）**刻意忽略**：它指名的模型会变，一条时而表示这个、时而表示那个的规则就得读而不是扫。读不懂（命令答了但那两行不在了）一律降级为不可用，绝不复用旧值、绝不显示 0；读不到（进程失败或 stdout 被污染）在 300 秒信任上限内保留上一次读到的窗口并在 5 秒后重试——这与会话列表的 `freshness` / `trustCeiling` 是同一套划分，理由也相同。运行目录钉在应用自己的目录下，其事件因此被监听器按 `cwd` 丢弃。**每 300 秒读一次**（当日 token 仍是 60 秒，见下一行——两者共享一次读取但各有节奏）。**自己留下的 transcript 只统计、不删除**：Settings 显示总大小并提供打开目录的按钮，删除由用户决定。**那个目录是反查出来的，不是拼出来的**——命名规则未公开，所以按一次已经发生的读取的 `session_id` 在 projects 下逐目录找 `<session_id>.jsonl`，第一次读取落地之前无从计数。Settings 那一行因此有两个非数字读数而不是干脆不画：读取未跑完时 `Calculating…`（按钮置灰），读取跑完却仍未找到目录时 `Unavailable`（CC-020）。对该私有目录始终保持只读。（曾考虑启动时自动清理并已否决：Claude Code 的 project 目录命名规则未公开，且压平分隔符与空格后并非一一对应——实测 `…/a b` 与 `…/a-b` 同属一个目录，因此该目录可能同时装着用户真实项目的会话记录。） | **Claude Code 私有输出格式。** 高版本风险——这是一段人类可读文本，措辞变化即失效。典型信号：两条规则同时显示 `--`；某条**已消耗**的规则有百分比却写着 `Reset unavailable`（未消耗且没有 reset 是 5 小时窗口尚未起算的正常状态，写作 `Not started`）；数字反复出现又消失；Settings 的 `Quota reading transcripts` 长期停在 `Unavailable`（读取跑完了但 `session_id` 反查不到目录，即 project 目录命名规则或 `session_id` 字段变了）；解析单测失败。CLI 更新后先比对那两行的前缀与 `N% used · resets ...` 的写法。**已记录的风险：** 用尽具名模型周上限的用户会看到两条健康的规则却仍被拒绝；若发生，正确修法是加第三个窗口，而不是让第二条改变含义。 | [`ClaudeCodeUsageReader.swift`](../Notchline/Notchline/ClaudeCodeUsageReader.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| 今日 token 用量 | 页脚显示 Claude Code 当日消耗的 token 总量，与 Codex 的数字并排。 | 官方没有任何接口报告当日用量；`/usage` 只给百分比窗口，不给绝对值。 | 只读 `~/.claude/projects/**/*.jsonl`，对每条 `assistant` 记录累加 `message.usage` 的 `input_tokens + cache_creation_input_tokens + cache_read_input_tokens + output_tokens`（ADR 0008 口径：处理过的全部输入含缓存读取，加输出），按 `timestamp` 前十位的 UTC 日期归桶。**读取成本是这项的全部设计**：mtime 早于**该 UTC 日**零点的文件不打开（与归桶用的是同一条线——本地零点会与它差一个时区偏移，UTC 以东少算、以西白读）；已扫过的文件只从上次停下的偏移继续读；不含 `"usage"` 字样的行从不解码；按 1 MiB 分块并在最后一个完整换行处停下（文件正在被追加，末行常是半行）。**分块用 `read(2)` 读进一个复用缓冲区，不用 `FileHandle`**：`FileHandle.read(upToCount:)` 交回的是 autoreleased `Data`，而这里是同步的 actor 方法、没有自己的 pool，于是每读 1 MB 进程就多占 1 MB 且不再归还——实测读 93 MB 涨 94 MB 并停在那里，同样的读取换成复用缓冲区只涨 0.2 MB。这就是应用内存随当天 Claude Code 用量水涨船高、且久久不落的原因。本 app 其余的 `FileHandle` 读取（transcript 的头尾各 64 KB）出于同一原因包在 `autoreleasepool` 里；`claude` 子进程的 stdout 管道原本也是，现在改成和这里一样按 `poll` + `read(2)` 读进复用缓冲区——那一处换掉 `FileHandle` 是为了给读取本身一个截止时间（CR-Fable-038，见 `tech-design.md` §15.2），不再需要 pool 只是顺带。文件变小视为被替换，整份重算。**以上三条都不是上限**（CC-009）：省下来的量仍然正比于 Claude Code 今天写了多少，而那个量不由本应用决定，所以每一遍还带一个 **128 MiB 的字节预算**——按实测流水线 600 MB/s 折算约 0.2 s 单核，约为实测最重一天（33 MB）的四倍。用尽预算的那一遍停在原地、保留已数的、返回"无数值"，下一遍从没读完的文件继续（已扫到当前大小的文件只花一次 `stat`），积压在几遍内排空；例外是「一行长过整份预算」——预算只在块之间生效，还没读出过一整行的文件会继续读到读出一行为止，否则那条记录会被每遍重读、每遍漏数。本机实测（2026-08-20，`-O`）：516 个文件共 163 MB，冷读（`F_NOCACHE`）全部只要 0.16 s（1.0 GB/s），整条流水线 440–630 MB/s；某一 UTC 日 00:20 时当日文件 72 个共 4.3 MB，一整个 UTC 日写出 33.2 MB。读不到目录返回"无数值"，读到但当日无记录返回 0，预算未跑完也返回"无数值"——三者都不是 0 以外的猜测，小的错数字看起来像清闲的一天。 | **Claude Code 私有只读 schema。** 中高版本风险。典型信号：今日用量长期显示 `--` 或明显偏小；单测失败。持续显示 `--` 且日志里有预算耗尽的 `notice`，说明 transcript 的量级已经超出这份预算的假设，应重新量一遍再调整，而不是把预算调大了事。CLI 更新后先检查 `assistant` 记录的 `message.usage` 四个字段名与 `timestamp` 格式。 | [`ClaudeCodeTokenCounter.swift`](../Notchline/Notchline/ClaudeCodeTokenCounter.swift)、[`ClaudeCodeUsageReader.swift`](../Notchline/Notchline/ClaudeCodeUsageReader.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| 会话工作状态（用户中断的唯一证据，以及审批被批准的唯一证据） | 用户在 Claude Code 里按 `Esc` 中断一轮之后，该行进入 Completed 而不是永远停在 Running 或 Approval needed；**批准一次审批之后该行立刻离开 Approval needed，而不是等到被批准的命令跑完。** | **官方 Hook 里没有任何取消或中断事件。** 实测 2.1.235（2026-08-18）：hook 事件表共 31 个事件，无一表示中断；查询循环的每条中断路径都在跑 `Stop` hook 之前返回（子 agent 的 `SubagentStop` 会跑，主 agent 没有对应物）。中断后到达的下一个事件是退出时的 `SessionEnd`。按 `AGENTS.md` §6.2 又不允许用计时器推断轮次结束，因此没有任何公开推送能回答这件事。 | 读官方公开命令 `claude agents --json` 输出里的 `status` 字段（词表 `busy` / `waiting` / `idle` / `shell`，另有 `waitingFor` 描述在等什么；命令与 `--json` 都是公开的，这些字段不是）。会话报告 `idle` 或 `shell` 时，`HookEventRepository.endTurnsForStoppedSessions(_:)` 把 reducer 手里该会话仍开着的轮次转入 Completed；`busy`、`waiting` 与**字段缺失**三种情况一律不动它。**该读数不携带任何轮次身份，因此永远不许开启、命名或描述一个轮次**，只许结束已经开着的那个。顺序护栏：读数必须整段晚于该轮次最后一个事件，比较用命令**开始**运行的时刻（`ClaudeCodeActivity.observedAt`）而不是它答复的时刻——列表最多缓存 30 秒，用答复时刻比较会让一次跨越提交瞬间的读取把它没看见的轮次报成空闲。词表之外的取值当作「没有报告」。实测同一台机器：提交后 150 ms 内 `busy`，审批对话框打开后 150 ms 内 `waiting`（`waitingFor: "permission prompt"`），对话框仍开着时按 `Esc` 后 160 ms 内 `idle`。**同一个字段还回答第二个问题：用户面前有没有对话框。** 人**批准**一次审批同样不产生任何 hook——`PermissionRequest` 之后的下一个事件是那次调用自己的 `PostToolUse`，而它落在工具跑完而不是对话框关闭的时刻（2026-08-23 实测 2.1.241，子智能体 sleep 12 秒：`PermissionRequest` +6.22 s，人在 +9.35 s 批准，`PostToolUse` +22.72 s，其间 13.4 秒错报 Approval needed）。`HookEventRepository.endApprovalWaitsForWorkingSessions(_:)` 因此在会话报告 `busy` 时结束该 thread 手里的审批等待——轮次自己的和每个子智能体槽位里的。**只认 `busy`**：`idle` 证明不了对话框不在（对话框仍开着时按 `Esc`，160 ms 内即到 `idle`），从缺席里读出结束会关掉用户正在看的对话框。顺序护栏在这里钉在每个等待自己的开启时刻（`PendingApproval.openedAt`）上而不是轮次的 `lastEventAt` 上。详见 [ADR 0011](adr/0011-a-turn-may-end-on-evidence-that-is-not-a-hook-event.md)。 | **Claude Code 私有输出字段。** 中版本风险，退化方向安全：字段消失或改名后一律读成「没有报告」，于是中断的行退回今天的样子（停在中断时的状态，直到该会话的下一条提示、会话退出或用户手动清除），不会产生错误状态。**危险的只有一个方向**：词表新增一个本应用擅自归入「不在工作」的词，那会把活着的轮次从用户眼前退休掉——所以未知词一律不认。审批那一半有自己的第二个危险方向：某个版本开始在对话框仍开着时报 `busy`，会把用户正在看的等待清掉——今天它整段报 `waiting`，包括父轮次 `Stop` 之后由子智能体弹出的对话框（2026-08-23 实测）。典型信号：中断后的行不再自己结束；或反过来，正在跑的行突然变成 Completed 又被下一个事件拉回 Running；或行在对话框仍开着时就离开 Approval needed。CLI 更新后先跑一次 `claude agents --json` 看 `status` 是否还在、取值是否还是那四个。**桌面端托管的会话不适用**：它把 CLI 跑成 `--output-format stream-json`，没有终端界面，而这个字段正是终端界面写出来的，所以那些会话永远没有 `status`（[#41](https://github.com/soondubu137/notchline/issues/41)）——那一半由下一行的 transcript 中断记录回答。 | [`ClaudeCodeSessionRegistry.swift`](../Notchline/Notchline/ClaudeCodeSessionRegistry.swift)、[`HookIntegration.swift`](../Notchline/Notchline/HookIntegration.swift)、[`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| 桌面端会话的中断记录（那种会话唯一的证据） | 由 Claude Code 桌面端托管的会话被用户中断之后，该行同样进入 Completed，而不是永远停在 Running 或 Approval needed。 | 上一行那份证据在这里不存在：桌面端把 CLI 跑成 `--output-format stream-json`，没有终端界面，而 `status` 正是终端界面写出来的——实测 2.1.234 的桌面端会话，其 `~/.claude/sessions/<pid>.json` 与 `claude agents --json` 的行里从生到死都没有 `status` 与 `updatedAt`（2026-08-19）。hook 那边同样什么都不来（见上一行）。于是整个公开面上没有任何东西报告这件事。 | 只读该会话的 transcript 末尾 64 KiB（位置与 `会话标题` 那一行同源），按一条**结构规则**找中断记录：`type` 为 `user`、`message.content` **恰好是一个 `text` 块**、没有 `promptSource`、没有 `isMeta`。命中后把记录的 `promptId`（= hook 的 `prompt_id`，2.1.237 实测两者同值）与 `timestamp` 交给 `HookEventRepository.endInterruptedTurns(_:)`：**只结束它指名的那个轮次**，指到 reducer 没在持有的轮次就什么也不做，也永远不许开启、命名或描述一个轮次。顺序护栏与上一行同源而更准——比较用记录被**写下**的时刻。**不读一个字消息正文**：解码结构里没有 text 字段（与 `AgentHookListener` 的解码器同理），只读块的 `type`。按 `(size, mtime)` 缓存，会话消失即丢弃；只问「不报告任何状态且此刻仍持有未结束轮次」的会话。边沿由 `ClaudeCodeMonitorService.transcriptWatcher` 按**文件**监听这些 transcript（中断不改写 `~/.claude/sessions/<pid>.json`，记录 watcher 看不到它；追加写也不触发目录级事件），它**不**把会话列表标记为过期。规则的测量与被它推翻的那段拒绝见 [ADR 0011](adr/0011-a-turn-may-end-on-evidence-that-is-not-a-hook-event.md) 的补充。 | **Claude Code 私有只读 schema。** 中高版本风险，退化方向安全：记录形状变了就一条也不命中，那些行退回今天的样子（停在中断时的状态，直到该会话的下一条提示、会话退出或用户手动清除）。**危险的只有一个方向**：某个版本开始把别的记录也写成「一个 `text` 块且没有 `promptSource`」，那会把还在跑的轮次退休掉——已知最接近的就是斜杠命令记录，它们把 content 写成**字符串**而不是块数组，2026-08-19 全机实测 205 个文件、7,438 条 `user` 记录：规则命中 15/15 条中断记录、其余 0 条（少问 content 形状的版本会多命中 202 条，其中 23 条后面模型还在同一 `promptId` 上继续工作）。典型信号：桌面端会话中断后行不再自己结束；或反过来，正在跑的桌面端行突然变成 Completed 又被下一个事件拉回 Running。CLI 更新后先看一条中断记录是否仍是 `user` + 单个 `text` 块 + 无 `promptSource`，以及 `promptId` 是否仍等于 hook 的 `prompt_id`。 | [`ClaudeCodeTranscriptReader.swift`](../Notchline/Notchline/ClaudeCodeTranscriptReader.swift)、[`HookIntegration.swift`](../Notchline/Notchline/HookIntegration.swift)、[`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`PathSetChangeWatcher.swift`](../Notchline/Notchline/PathSetChangeWatcher.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| 桌面端会话的审批应答（那种会话唯一的证据） | 由 Claude Code 桌面端托管的会话上，**批准一次审批之后该行立刻离开 Approval needed**，而不是等到被批准的命令跑完。 | **没有任何 hook 对应「人按下批准」。** 2026-08-23 对 2.1.241 把**全部 31 个** hook 事件注册到一份一次性 settings 上实测：从开启对话框的 `PermissionRequest` 到那次调用自己的 `PostToolUse`（26 秒之后），其间唯一到达的事件是另一个无关 agent 的 `SubagentStop`。而 `PostToolUse` 落在**工具跑完**的时刻，不是对话框关闭的时刻。上一行那份会话状态在这里同样不存在（桌面端没有终端界面，因而永远没有 `status`），所以整个公开面上没有任何东西报告这件事。 | 只读 `~/Library/Logs/Claude/main.log` 末尾一段（位置与 `Desktop 已读后自动移除终态会话` 那条焦点读数同源，但**各自持有游标**——两个问题共用一个偏移量，先问的那个会把后问的那行吃掉），匹配两种行形并用 request id 把它们配对：`Emitted tool permission request <id> for <tool> in session local_<desktop id>` 与 `Received permission response for <id>: <decision> (tool: <tool>)`。**只有前者带会话、只有后者证明人答过**，所以配不上开启行的应答一律不归属给任何会话（失败方向是等待继续留着）。**决定本身不读**：`once`、永久允许还是拒绝都同样是人答过了，而本应用没有任何理由去关心用户选了什么。Desktop 的 id 到 CLI 的 id 由 Desktop 自己的记录（`sessionId` ↔ `cliSessionId`）连接，与已读那条同源，因此不猜任何 id。答案交给 `HookEventRepository.endAnsweredApprovalWaits(_:)`，与终端会话那条 `busy` 读数**同一个入口**：两份证据说的是同一句话——「此刻这条 thread 没有对话框在用户面前」。顺序护栏钉在每个等待自己的开启时刻上；日志时间戳是本地时区、秒级精度，截断方向安全（只会让应答显得更旧而被拒绝）。**只在真的有审批开着时才读**：没有等待就不读日志、不读 Desktop 记录树、也不监听文件。边沿由 `ClaudeCodeMonitorService.permissionLogWatcher` 在那段时间里按文件监听该日志——焦点读数当初拒绝监听它是因为「一条 oauth 查询也要唤醒一次」，那条理由只在没有东西等着它时成立。 | **Claude Desktop 私有日志正文。** 高版本风险，退化方向安全：行形改了就一条也不命中，那些行退回今天的样子（批准后继续写着 Approval needed 直到被批准的调用自己关闭）。**危险的只有一个方向**：某个版本把「已应答」写在对话框其实还开着的时候，那会关掉用户正在看的那一个——今天这三行是同一秒里成对出现的。典型信号：桌面端会话批准之后行不再自己恢复；或反过来，行在对话框仍开着时就离开 Approval needed。Claude Desktop 更新后 `grep 'Emitted tool permission request' ~/Library/Logs/Claude/main.log` 看行形是否还在。**终端托管的会话不走这条**：它们由上一行的 `status` 回答，那是本应用更信任的读数。 | [`ClaudeDesktopPermissionLog.swift`](../Notchline/Notchline/ClaudeDesktopPermissionLog.swift)、[`HookIntegration.swift`](../Notchline/Notchline/HookIntegration.swift)、[`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| `~/.claude/sessions/` 作为「该复核了」的信号 | 会话增删时立刻重新查询官方会话列表，而不是等心跳。**一个 Claude Code 都没开的时候，它是唯一会让本应用再去查询的东西**——那种情况下的列表被扣住，不再按节拍重查（CR-Fable-002）。 | 官方没有提供会话增删的通知接口；`claude agents --json` 是查询而非推送。 | 用现有的 `DirectoryChangeWatcher` 监听该目录，**只当作变更信号，从不解析目录内容**。权威数据永远来自官方命令 `claude agents --json`。边沿在唤醒刷新之前先调用 `ClaudeCodeSessionListing.invalidate()`，否则被唤醒的那次刷新仍然会回答一份最多 `freshness` 那么旧的缓存——桌面端会话正是被第一条提示创建的，整轮都落在那个窗口里（见 `tech-design.md` §15.1）。watcher 由 `ClaudeCodeMonitorService` **持有**，并在每次刷新时 `attachIfNeeded()`：`AsyncStream` 不持有产出它的对象，而该目录在 Claude Code 首次运行前并不存在。**挂上这件事本身也被当作一条边沿**（CR-Fable-002）：一份「已经答出来是空的」会话列表现在被扣住、不再按 `freshness` 重读，而在 watcher 挂上之前扣住的那份「空」是瞎读来的——先注册 hook 的用户启动本应用时这个目录还不存在，他开的第一个会话正是创建它的那一个。watcher 送不出这条消息（挂上只推进它自己的 `changeCount`，没有文件系统事件可以 yield 给流），因此由服务在刷新里认这个跃迁并 `invalidate()`（见 `tech-design.md` §15.1）。 **该目录只报告会话的生灭，不报告它们的状态变化**：CLI 会话每次 `busy` / `waiting` / `idle` 翻转都会重写自己那份记录，但它是原地重写、不改名，而目录级 vnode 事件不为「目录里某个文件被写」触发（2026-08-18 用本应用的事件掩码实测：原地重写 0 次事件，新建／删除／原子替换各 1 次）。因此工作状态的变化由**另一个 watcher 监听记录文件本身**（`ClaudeCodeSessionRecordWatcher`）：监听**列表里每一个会话**的 `<pid>.json`，**同样只当作变更信号、从不解析文件内容**，权威数据仍然只来自 `claude agents --json`。这里的私有知识只有一处：记录文件按 pid 命名，而 pid 由那条公开命令给出。**监听范围曾经收窄到「轮次仍在跑」的会话，被一条实测推翻**：`/clear` 与会话内的 `/resume` 会原地换掉会话 id——同一个 pid、同一份记录、同一个 inode，只有里面的 `sessionId` 变了（实测 2026-08-21：记录仍写着 23:14 启动的那个进程，其 `sessionId` 的 transcript 从 01:14 才开始）。既不新建也不删除文件，目录边沿看不见，而那一刻正好是这个会话没有轮次在跑的时候，于是列表在整个 `freshness` 窗口里报着旧 id、hook 带着新 id，整轮画不出任何一行。代价由这些文件的安静兜住：实测 2026-08-21，每秒采样、90 秒、六个会话（两个坐在提示符前），没有一份记录被重写过。另有一条不依赖任何文件的兜底写在服务里：reducer 握着的轮次若在读数开始之后还动过、而列表不认得它的会话，就以轮次为准作废列表并在同一次刷新里重读（见 `tech-design.md` §15.1）。**同一条命名规则另有一个只关于本应用自己的用途**：`claude -p "/usage"` 是一个真会话，进出各写／删一次 `<pid>.json`，两条目录边沿都会让列表作废，于是每次用量读数买回一次 `claude agents --json`（一个 Node 进程、约 0.4 s；实测启动后 3.5 s 那一次就是它）。现在目录边沿先比一遍**条目名**——只有名字，不读任何文件内容——进出的名字若全部以本应用自己启动的那个 `claude` 的 pid 打头（pid 来自本应用的 `Process`，不是从文件里读出来的），就不作废。其余一切情况照旧作废：名字没变、名字不认得、目录读不出来。权威数据仍然只来自 `claude agents --json`，这里没有新增任何 schema 依赖。 | **私有目录位置与文件命名，非 schema。** 中低风险，退化方向安全但**不再只是变迟钝**（CR-Fable-002）：一份已经答出来是空的会话列表不再按 `freshness` 重读，所以目录消失、改名、或 watcher 静默失效时，没有任何时钟会把它拉回来。兜底有两条，都不依赖这个目录：会话一提交提示，那条 hook 事件就会让列表作废并在同一次刷新里重读（见本表下方的服务侧兜底与 `tech-design.md` §15.1）；列表非空时仍按 `freshness` 重读，因此已经画出来的行不受影响。真正丢掉的只有「开着、但一次提示都没提交过」的会话在刘海上的那个存在标记——少报，不误报。文件命名规则变化只会让被中断的行等到下一次刷新（最坏一个心跳），也让 `/clear` 之后的那一轮退回「等下一次读数」——但那一轮仍会被服务里那条不依赖文件的兜底救回来，同样不产生错误状态。 | [`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`ClaudeCodeSessionRecordWatcher.swift`](../Notchline/Notchline/ClaudeCodeSessionRecordWatcher.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| Desktop 已读后自动移除终态会话 | Claude Desktop 托管的会话在用户读过之后，其 Completed 行自动从列表消失；用户在 Desktop 归档该会话同样移除。Running、Input、Approval 不受影响。**终端里的会话由下一行单独回答**——它们仍然没有已读概念，但它们的终端有（见 [ADR 0012](adr/0012-read-state-is-answered-per-product-or-not-at-all.md)）。 | 官方 Hooks、`claude agents --json` 与 deep link 都不报告「用户是否看过某个会话」。Hook 只描述已经发生的生命周期事件；会话列表只有身份与工作状态；transcript 记录写入而不记录阅读。Claude Code 自身也不落盘任何 focus/read 字段（2.1.235 二进制内检索无此类键）。 | 只读 `~/Library/Application Support/Claude/claude-code-sessions/<org>/<account>/local_<uuid>.json`，按 `<org>/<account>` 恰好两级枚举，可用 `NOTCHLINE_CLAUDE_DESKTOP_HOME` 覆盖根目录。每份记录只解码 `cliSessionId`（= hook 的 `session_id`，身份连接不需推断）、`sessionId`（Desktop 自己的 `local_<uuid>`，即它的日志在屏幕上点名的那个 id）、`lastFocusedAt`、`isArchived` 四个字段，标题/`cwd`/MCP 配置一律不解码。判定为 **`lastFocusedAt` 晚于该 Turn 自己的终止时刻即已读**——比较左边取自本应用的 reducer 而不是同一文件里的 `lastActivityAt`，因此桌面端的活动记账延迟或停写都不能把一个轮次说成已读；`isArchived` 同样算已读；**没有记录就是 unknown，不是未读**，那一行既不被隐藏也不进未读 gate。`lastFocusedAt` 由安装包里的 `setSessionVisibility(id, isVisible)` 在会话被显示时写成 `Date.now()` 并立即落盘，落盘是同目录临时文件 `rename`，因此用**目录级** `O_EVTONLY` watcher（`PathSetChangeWatcher`，50 ms trailing debounce）监听状态根目录与每个账户目录。按 `(size, mtime, inode)` 缓存解析结果，只打开变化过的记录（本机 31 份约 2 MB：首读 5 ms，全命中 1 ms）；记录数超过 512 时按 mtime 取最新的 512 份。**整遍包在 `autoreleasepool` 里，而且没有终态行的时候整遍不跑**——这份读数只可能改变终态行的去留，一份没有 `.completed` 行的列表原本每次刷新都要白读一遍整棵树（CR-Fable-041，实测数字见 [`system-architecture.md`](system-architecture.md) §6）。拒绝 symlink、非当前用户普通文件与超过 4 MiB 的文件；不写入、不连接任何 IPC。**另外三条判定路径（编排层，不新增任何私有 schema）：** 实测表明用户切回窗口读一个已经在屏幕上的会话，磁盘上不留任何痕迹（整棵树 0 个文件变化），安装包里也没有任何已读字段，因此文件之外另加三条：Claude Desktop 在该 Turn 结束**之后**回到前台；或 Claude Desktop **此刻持有前台**且屏幕醒着、未锁、无屏保、登录会话在 console；两者都要求**屏幕上的就是这个会话**（记录里 `lastFocusedAt` 最大的那一个，且日志没有否决，见下）。第三条是该会话曾带着已结束的 Turn 停在屏幕上（由本应用逐次刷新实时观察，不从时间戳回溯），而 Desktop 此后把别的会话放了上去。激活与前台归属都来自**公开 API** `NSWorkspace.didActivateApplicationNotification`（构造时从 `NSWorkspace.frontmostApplication` 读一次种子），三条排除读数来自**公开 API** `CGDisplayIsAsleep`、`CGSessionCopyCurrentDictionary` 与屏保的分布式通知（均无需 entitlement，实测不弹授权）。本表要登记的仍然只有过滤条件所用的 **bundle identifier `com.anthropic.claudefordesktop`**——该标识未作为兼容契约公开，与 Codex 侧 `com.openai.codex` 同性质。第二条取「回到前台」的跃迁时刻；第三条读「此刻持有前台」这个状态，并且只对**屏幕上的那一个**会话成立——把窗口留在前台后离开的用户会被替他读掉，这是经产品明确接受的代价（ADR 0012 第三条）。不读窗口标题、不读窗口几何、不用 Accessibility、不做 GUI 自动化。**「屏幕上是哪个会话」有第二个来源，因为记录只答得出一半：** `setSessionVisibility` 只在会话被放上屏幕时盖章，会话被拿下来时什么都不写，所以用户切到**新会话的输入框**之后，最后被盖章的会话继续冒充在屏幕上——实测 2026-08-19 晚：21:03:30 打开输入框（磁盘 0 变化）、21:03:58 轮次结束、21:04:18 新会话一开始，那一行就被当作读过撤掉了。第二个来源是 Claude Desktop 自己的日志 `~/Library/Logs/Claude/main.log`（可用 `NOTCHLINE_CLAUDE_DESKTOP_LOG` 覆盖）里的 `[CCD] LocalSessions.setFocusedSession: sessionId=<local_uuid>` 或 `=null`——安装包 `1.32885.1` 里 `setFocusedSession(e){ log.info(…e ?? \`null\`…) }` 无条件写出，每次导航一行，**`null` 正是记录写不下来的那一半**。只从本进程启动时的末尾向前读（`AGENTS.md` §6.2：只认启动之后观察到的事件），启动时与轮转后的那段历史只读一次且**只允许它说 `null`**；单次最多 256 KiB，只解析到最后一个换行为止（半行会带半截 id），只匹配这一种行、只取 `sessionId=` 之后那一段；`local_<uuid>` 到 hook 身份的连接来自记录里的 `sessionId` 字段，接不上就读成「不是这个会话」。**它在编排器里只是一个否决权**：能拦下记录声称的会话，不能提名记录没声称的会话，因此这份依赖只会让行多留，永远不会让行提前消失。 | **Claude Desktop 私有只读 schema，外加未公开的 bundle identifier 与一份未公开的日志行格式。** 高版本风险。验证于 Claude Desktop `1.32885.1`、CLI `2.1.235`，2026-08-19（表头基线未随之推进：本行之外的行未在该版本上重验）。典型信号：用户在 Claude Desktop 读过之后 Completed 行仍然留着；诊断包含「Claude Desktop 会话状态 schema 不兼容」；对应单测失败。fail closed：树不存在是纯终端用户的常态，报告 `unavailable` 且**不产生诊断**；单份记录读不出只让那个会话答 unknown；全部记录都读不出才判为 schema 不兼容，保留 last-known-good 且不做任何新的隐藏。Desktop 更新后首先检查 `local_*.json` 是否仍在该路径、是否仍带 `cliSessionId`、`sessionId` 与 `lastFocusedAt`，以及写入是否仍是原子替换；日志那一半的失效信号是「在新会话的输入框里等着的时候，旧的终态行又开始提前消失」，fail closed 的方向是 `unknown`——日志不存在、读不出、行的形状变了，一律退回本行其余部分的原样行为，不会提前撤行；bundle identifier 变化的信号是「切回 Claude Desktop 后行仍然不消失」，且此时第一条路径（切换会话）与「被别的会话顶下去」仍然有效，因此只会退化不会误移除。 | [`ClaudeCodeDesktopReadState.swift`](../Notchline/Notchline/ClaudeCodeDesktopReadState.swift)、[`DesktopActivationWatcher.swift`](../Notchline/Notchline/DesktopActivationWatcher.swift)、[`DesktopReadingWatcher.swift`](../Notchline/Notchline/DesktopReadingWatcher.swift)、[`DesktopDisplayedSession.swift`](../Notchline/Notchline/DesktopDisplayedSession.swift)、[`PathSetChangeWatcher.swift`](../Notchline/Notchline/PathSetChangeWatcher.swift)、[`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| 终端会话已读后自动移除终态行 | 从终端启动的 Claude Code 会话，在用户回到那个终端之后其 Completed 行自动从列表消失。Running、Input、Approval 不受影响。 | 官方 Hooks、`claude agents --json` 与 deep link 都不报告「用户是否看过某个会话」，Claude Code 自身也不落盘任何 focus/read/seen 字段（2.1.236 二进制内检索 0 命中；进程里的 `userPresence` 只活在内存）。上一行的 Desktop 记录对终端会话完全不存在。而「哪个 tab 在用户眼前」不可能从进程外得知，本应用也不去猜。 | **不问 Claude Code，问那个终端。** 三个公开 BSD 调用：`sysctl(CTL_KERN, KERN_PROC, KERN_PROC_PID, pid)` 取 `kp_eproc.e_tdev`（pid 来自公开命令 `claude agents --json`），`devname_r` 把设备号变成 `/dev/ttysNNN` 并**回查** `st_rdev` 一致，`stat` 取 `st_atimespec`——最后一次**该会话从这个设备读取**的时刻（2026-08-20 实测更正：一次返回 `EAGAIN`、零字节的 `read` 同样让它前进，而 `write`/`open`/`close`/`ioctl`/`select` 都不动它；它记的是会话读过，不是有人做过什么）。第四个调用是同一个 `sysctl` 的 `kp_eproc.e_ppid`，逐级向上（最多 16 级）看**前台应用的 pid 是不是这个会话的祖先**——`claude` → 登录 shell → `login` → 那个终端；前台 pid 由公开的 `NSWorkspace.didActivateApplicationNotification` 维护，并复用 `DesktopReadingWatcher.systemScreenIsAvailable`（显示器醒着、未锁、在 console）。判定：**该时刻 ≥ 该 Turn 的终止时刻，且那个终端的应用此刻在人眼前，才算已读**，比较左边与 Desktop 那四条同源，并且**与那四条平级、任一成立即已读**，不是它们的兜底——开着**远程控制**的会话同时摆在终端与 Claude Desktop 面前，两边由不同手势读，在终端里读它不会让 Desktop 的记录动一下。反方向安全且是结构性的：这一条只可能对真有控制终端的会话成立，而 Desktop 托管的会话走管道（`--output-format stream-json`，无终端界面）因此没有。实测 2026-08-19：开着远程控制的 CLI 会话在 `claude-code-sessions` 树里没有记录，整棵 Claude Application Support 树里也没有任何文件提到它的 `sessionId` 或 `bridgeSessionId`——Desktop 对它的了解走服务端的桥，不落本地盘。不打开任何文件内容、不读窗口标题或几何、不碰 Accessibility、不做 GUI 自动化、不写任何东西。**登记的私有依赖有两条，都是从 CLI 的终端行为里观察到的，不是任何公开契约。** 其一，让这条路径覆盖到「切走再切回」的，是 Claude Code 自己打开的焦点上报（`ESC [ ? 1004 h`，在发布二进制里，每次离开 alternate screen 写一次）。其二，**Claude Code 还打开全动作鼠标上报**——`ESC [ ? 1000 h`、`1002 h`、**`1003 h`**、`1006 h`，启动时写一次、会话中途再写一次（`2.1.238` 抓包）——于是终端为指针在窗口上的每一次移动写字节进 pty，**不需要焦点**。上面那半句「且那个终端的应用此刻在人眼前」正是为它加的：没有它，双屏下指针只是路过另一块屏上那个从未获得焦点的终端窗口，一行没人读过的终态就在一秒内消失且不可逆（gate 对已结束的轮次隐藏是终局，CC-024）。访问时间是一个标量，事后分辨不出推动它的是按键、焦点上报还是鼠标上报，所以这一层修不了，只能与前台**同时**要求。设备访问时间不产生文件系统事件，因此在 gate 已为等待行预约的 1 秒复查上采样（每行每秒一次 `sysctl` 加一次 `stat`，没有行在等时一次也不问）。答 `nil`（没有控制终端）的会话**不进 gate**，保持既有退出方式。实测 2026-08-19（Ghostty + `2.1.236`）并于 2026-08-20 在 `2.1.238` 上重测：敲键、拿到前台、失去前台、**指针划过**四种都让访问时间前进，**隐藏的界面经历两轮完整前台切换 0 次变化**，CLI 渲染输出只动修改时间（每秒约 2 次）；在自建 pty 上跑完一整轮（提交→答案→Stop hook→通知与响铃），访问时间只在提交那一下前进、此后 110 秒不变，**轮次结束本身不动它**——这条单独验过，它不成立整条路径就会在没人读的情况下撤行；会话空转 150 秒 0 次变化。 | **观察到的 CLI 终端行为，非 schema、非路径、非字段。** 低风险，退化温和且方向单一：焦点上报若被去掉，只剩按键，行等的是用户下一次敲键而不是回到 tab——**不会误移除**。典型信号：切回终端后行不消失，而敲一下键就消失。CLI 更新后先 `strings` 查 `?1004h` 与 `?1003h` 是否还在，再确认 `claude agents --json` 的 `pid` 仍是该进程本身。**已知的洞**：远程控制的会话若在 Claude Desktop 或手机上读完而不碰终端，行会留着——两边都没有本地证据，而用户观察到的「CLI 里读完 Desktop 未读标记也消失」是服务端同步，本地不落盘。方向安全（留着而不是提前消失），且下一次在终端里做任何事或从任一边提交下一轮都会收敛。**新增一处退化，方向仍朝保留**：`tmux`、`screen`、`ssh` 里的会话向上走链只通到 `launchd`，宿主永远不持有前台，因此第五条对它们不再成立，它们改为按下一次提交 / 会话消失 / 手动移除退出。**仍朝提前移除倒的路径**：其一，指针在**确实持有前台**的终端窗口上移动算已读（与 Desktop 那条「窗口摆在空椅子前」的取舍相同，而且更窄——还要求有人动了指针）；其二，前台是在手势之后约一秒才采样的，用户在 `ESC [ O` 与 workspace 通知之间那几毫秒离开终端会被读成没有离开；其三，CLI 自己发的终端查询收到回复也会推进访问时间——实测这些回复全部落在启动那几百毫秒，那时还没有轮次结束过，但将来若有版本在会话中途重新查询，表现就是一行提前消失。抢前台造成的 `ESC [ O` 不再是其中一条：采样那一刻持有前台的是抢走它的那个应用。 | [`ControllingTerminalGestures.swift`](../Notchline/Notchline/ControllingTerminalGestures.swift)、[`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| 点击 Claude Code 行时唤起它的宿主 | 点击一行 Claude Code 会话：桌面端托管的激活 Claude Desktop，终端里的把那个会话所在的标签页带到前台（终端报不出 tty 时只激活该终端应用），并把用户带到那扇窗户所在的桌面。不要求定位到具体会话。 | 没有任何受支持的接口能聚焦一个**已经存在**的 Claude Code 会话：官方 [deep links](https://code.claude.com/docs/en/deep-links) 只有 `claude-cli://open`（参数只有 `q`/`cwd`/`repo`，总是新开窗口和新会话），Desktop 侧公开的 `claude://code/new` 同样只新建；实测 `claude://code/sessions/local_…` 被 Desktop 以 `unrecognized code path` 拒绝。`claude agents --json` 给出 `pid` 与 `cwd`，但不说这个会话属于哪个应用。 | **登记的私有依赖只有一条：怎么认出「这是桌面端托管的会话」。** 从会话 pid 用 `sysctl(CTL_KERN, KERN_PROC, KERN_PROC_PID)` 的 `e_ppid` 与 `proc_pidpath` 向上走进程祖先链（两者都是公开 BSD/libproc 调用），每个祖先取它最外层的 `.app`，祖先中出现 bundle id `com.anthropic.claudefordesktop` 即判定为桌面端托管——**该 bundle id 与「桌面端会话是 `Claude.app` 的后代」这个进程形态都不是公开契约**，与上面已读那两行同性质。实测 2026-08-19：`claude`(46868) → `Claude.app/Contents/Helpers/disclaimer`(46867) → `Claude.app`(24014)。两处细节是踩过的坑：走链**从父进程开始**，因为桌面端的 `claude` 自己就跑在 `~/Library/Application Support/Claude/claude-code/<version>/claude.app`（`com.anthropic.claude-code`）里，把会话进程本身算进去会让每个桌面端会话都自己托管自己；命中后取该 bundle id 在链上**最高**的那个祖先，因为最近的那个是 helper，窗口服务器不把它当应用。判定为终端时，宿主就是最近的那个 `.app`（实测 `claude`(60817) → `zsh`(880) → `login`(877) → `Ghostty.app`(665)），标签页则完全走**终端自己公开的脚本字典**：Terminal.app 的 `tab` 有 `tty`、iTerm2 的 `session` 有 `tty`，按控制终端设备（与终端已读同一条 `sysctl` + `devname_r` 路径）匹配后 `select`。Ghostty 1.3.1 的 `Ghostty.sdef` 里整份没有 tty，只有标题与工作目录，按之匹配是 PRD 禁止的猜测，因此只激活应用。**唤起时跟着窗口换桌面的那一步全部走公开 API，不新增任何私有依赖**：`CGWindowListCopyWindowInfo([.optionOnScreenOnly, .excludeDesktopElements])` 问该 pid 在当前 Space 有没有可见窗口，没有就 `NSRunningApplication.hide()` 再 `activate()`（应用自己把窗口 order front 才会带走用户，`tech-design.md` §14.2 有逐条实测）。不读 `~/.claude/sessions/<pid>.json`（它带 `entrypoint` 且答案一致，但那是私有 schema，只作旁证），不修改任何东西，不碰 Accessibility，不做 GUI 自动化，不连 `/tmp/cc-socks/*.sock`。 | **观察到的进程形态加一个未公开的 bundle identifier，非文件 schema。** 低风险，退化温和且方向单一：桌面端换了启动方式或换了 bundle id，祖先链上找到的最近应用就变成别的东西，最坏结果是**激活了错误的应用**或答 `hostUnknown` 让这一次点击失败——不会误报成功，也不会打开别人的会话。典型信号：点桌面端托管的行唤起的不是 Claude Desktop；或点击后提示「无法确定该会话运行在哪个应用里」。Desktop 更新后先用 `ps -o ppid=` 走一遍某个桌面端会话的祖先链，确认 `Claude.app` 仍在其中且 bundle id 未变。**另有一条与 Claude 无关的失效**：终端换了脚本字典（Terminal.app / iTerm2 的 `tty` 属性）——那是终端各自的公开契约，坏了也只是退回到只激活应用。 | [`ClaudeCodeNavigator.swift`](../Notchline/Notchline/ClaudeCodeNavigator.swift)、[`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`NotchlineTests.swift`](../Notchline/NotchlineTests/NotchlineTests.swift) |
| 子智能体仍在跑时行尾说出来 | 与 Codex 表里同名那一行是同一件事同一套画法：一条会话自己的轮次结束之后，如果它派生的子智能体还在跑，该行进入 Completed 并在计时腾出来的位置写 `1 subagent` / `N subagents`，收起态按 Running 汇总并写出全表计数，排序进 Running 那一档，已读也不把它移除，而最后一个子智能体收尾之后 settling 窗口从**那一刻**起算。 | 事件名 `SubagentStart` / `SubagentStop` 与字段 `agent_id` 都在随 CLI 二进制分发的官方 hook input schema 里（`agent_id` 在每一条 input 都继承的 base schema 上，官方描述为「只在 hook 从子智能体内部触发时出现……用这个字段而不是 `agent_type` 来区分子智能体调用与主线程调用」），**这一半是公开的**。不公开的是**子智能体的 hook 用哪一条会话与哪一个轮次盖章**：官方文档没有说过它盖的是父会话的 `session_id` 与父轮次的 `prompt_id`。而本应用把 `session_id` 当作 Thread 主键、把 `prompt_id` 当作 Turn 主键，所以「子智能体的事件会落在父会话那一行、而且落在它当前那个轮次上」这条前提必须靠实测确定。官方公开面上也没有别处可问：`claude agents --json` 只报会话的身份与工作状态，不报它派生了几个子智能体。 | 实测 2026-08-23（Claude Code CLI `2.1.241`，一次 `-p` 运行，提示词明说不要等待那次 `Agent` 调用）：`Agent` 的 `PreToolUse`/`PostToolUse` 相继立刻到达 → `SubagentStart`（带 `agent_id` 与 `agent_type`）→ 主智能体的 `Stop`，带 `background_tasks: [{id: <agent_id>, type: "subagent", status: "running"}]` → 子智能体自己的 `PreToolUse`/`PostToolUse`（带同一个 `agent_id`）→ `SubagentStop`。全程 `session_id` 与 `prompt_id` 都是父会话与父轮次的。据此：注册 `SubagentStart` 与 `SubagentStop`（定义总数 11 → 13）；reducer 里两条边界按 `agent_id` 加减该 Thread 当前 Turn 上的 `runningSubagentIDs`，不推进 `lastEventAt`；**带 `agent_id` 的其他事件一律消费后丢弃**（成对丢，否则失信探测误报），`MessageDisplay` 的折叠也照这条丢。 | **一半公开 schema，一半实测行为。** 中版本风险，失效方向安全：若某次更新改成用子智能体自己的 `session_id` 或自己的 `prompt_id` 盖章，那些事件会落到一个永远不成行的会话上——行照常在主智能体的 `Stop` 上进入 Completed，只是行尾不再出现 `1 subagent`。`agent_id` 改名或消失退化成同一个样子。典型信号：派生过子智能体的 Claude Code 行不再显示 `N subagents`。**加重的方向只有一个，并且是明写接受的**：`SubagentStart` 到达而 `SubagentStop` 永久丢失时，整个收起态会长期停在 Running 并写着一个不会归零的数字，直到该会话离开列表或用户右键移除该行（[`PRD.md`](PRD.md) §6.2，[#102](https://github.com/soondubu137/notchline/issues/102)）。**那条曾经写在这里的缺陷已经修掉**：子智能体自己的 `PermissionRequest` 此前被 `agent_id` 闸门吞掉，行写着 Running 或 `N subagents` 而产品其实停在对话框上；现在这些事件进入该子智能体自己的一格，行尾那一格转亮、收起态按 Approval needed 汇总，详见本表下一行与 `tech-design.md` §9.2。CLI 更新后先 `strings -a ~/.local/share/claude/versions/<版本>` 查 `hook_event_name:Ct("SubagentStart")` 是否仍在、base schema 里 `agent_id` 是否仍在。**另有一条同一行的显示后果，它依赖的那个字段是公开的，因此没有单独成行**：`Stop` 的 `background_tasks` 在官方 hook input schema 上并带着官方描述（「让 hook 区分『会话结束了』与『会话正暂停等待后台工作把它叫醒』，没有活在飞时是空数组」），本应用只读它是不是空的，不读条目里的任何字段；子智能体收尾之后 Claude Code 会用一个新的 `prompt_id` 把父轮次叫醒，实测间隔 50 ms（`-p` 下 130 ms），这个读数是那 50 ms 里唯一能拦住行闪一次 `Completed` 的东西（`PRD.md` §6.2 第 3 档、探索文档第 12 节）。该字段消失时退回闪一次，不伪造任何状态；加重的方向与上一句那条同向同出口：父轮次永远不被叫醒时行停在 `Running`。 | [`HookIntegration.swift`](../Notchline/Notchline/HookIntegration.swift)、[`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`MonitorDomain.swift`](../Notchline/Notchline/MonitorDomain.swift)；测试 `aClaudeCodeSubagentOutlivingItsTurnLeavesTheRowFinishedAndStillWorking`、`aReadClaudeCodeSessionWithASubagentStillRunningKeepsItsRow`、`aSubagentsDisplayedTextIsNotTheRowsPreview`、`theDerivedStatusReadsTheCountAndNotTheProduct`、`aTurnPausedForItsSubagentDoesNotSayFinishedBetweenTheTwo`、`aTerminalThatNamesNoBackgroundWorkIsTheThreadFinishing`、`claudeCodeVocabularyMapsItsOwnEventNames` |
| 子智能体停在对话框上时行会说 | 与 Codex 表里同名那一行是同一件事同一套画法，且由同一段 reducer 代码实现。 | 事件名与 `agent_id` 都在官方 schema 里（`agent_id` 在每一条 hook input 都继承的 base schema 上），**这一半是公开的**；不公开的是子智能体的 `PermissionRequest` 用哪一条会话与哪一个轮次盖章。**另有一件是公开 schema 反而会让人读错的**：`PermissionDenied` 带着被拒调用的 `tool_use_id`，看上去「拒绝会精确关闭」，实际不是。 | 实测 2026-08-23（CLI `2.1.241`，pty 交互式会话，一次性 `--settings` + `--setting-sources project`，四次运行）：`PreToolUse`(`agent_id`, `tool_use_id`) → 20 ms 后 `PermissionRequest`(`agent_id`，无 `tool_use_id`)；`session_id` 与 `prompt_id` 都是父的；批准由 `PostToolUse`(同一对) 精确关闭；**人拒绝什么都不发**——`PermissionDenied` 在二进制里只有一个产生点，且唯一调用点被 `decisionReason.classifier == "auto-mode"` 挡着，它报的是自动模式分类器拒绝而不是人。据此 `reportsApprovalDenials` 由 `true` 改为 `false`，借用 id 的等待在主线程与子智能体那一格上都靠「同一个 agent 的别的调用上有活动」关闭。 | **一半公开 schema，一半实测行为。** 中版本风险，失效方向安全，与 Codex 表同名那一行相同。**特别要盯 `PermissionDenied` 的门**：若某次更新让它也为人的拒绝触发，本应用只会多一个精确关闭点，不会出错；反过来若 `PermissionRequest` 不再为子智能体触发，行退回「只写 `N subagents`、不转亮」。CLI 更新后先 `strings -a ~/.local/share/claude/versions/<版本>` 查 `hook_event_name:Ct("PermissionDenied")` 那个产生点还在不在同一个门后面。 | [`HookIntegration.swift`](../Notchline/Notchline/HookIntegration.swift)、[`ClaudeCodeMonitorService.swift`](../Notchline/Notchline/ClaudeCodeMonitorService.swift)、[`MonitorDomain.swift`](../Notchline/Notchline/MonitorDomain.swift)、[`NotchOverlayView.swift`](../Notchline/Notchline/NotchOverlayView.swift)；测试 `neitherProductReportsAHumanRefusalSoBothInferIt`、`aRefusedMainThreadCallStopsSayingApprovalNeeded`、`aSubagentsApprovalOutranksTheRunningItAlsoIs`、`aRowWithASubagentAwaitingApprovalIsStillDismissable` |

## 不在本表中的能力

- 实时 Turn 生命周期桥、Hook 安装/升级/总开关/移除使用官方 Codex Hooks、公开事件与 `hooks.json` 配置，不在本表记录。
- Claude Code 的会话**发现**使用官方公开命令 `claude agents --json`——其 `--help` 明确承诺 `--json` 打印包含交互式在内的活动会话且不需要 TTY——不在本表记录。同一份输出里的 `status` / `waitingFor` 是另一回事：命令公开，这两个字段的存在、词表与语义都不公开，且它们决定业务状态，因此**单独登记在上表**。
- Claude Code 的 Hook 事件、`type: "command"` handler（含 `args` exec form 与 `timeout`）与 `~/.claude/settings.json` 中的 `hooks` 配置位置均为官方公开能力，不在本表记录。本应用**会写**该文件的 `hooks` 键（见 [ADR 0016](adr/0016-write-the-users-claude-code-settings-and-keep-a-copy.md)，取代了此前「永不写入」的 [ADR 0010](adr/0010-never-write-the-users-claude-code-settings.md)），写入前把原文件复制到同目录的 `settings.json.notchline-backup`（`~/.codex/hooks.json` 同理，复制成 `hooks.json.notchline-backup`）。写的是官方公开的配置位置与公开的 handler 形状，因此仍不构成私有依赖。该 handler 指向的 helper 与它连接的 Unix domain socket 都在本应用自己的 support 目录里，`/usr/bin/nc` 是 macOS 自带工具，三者都不构成私有依赖。此前用的是 `type: "http"`，同样是公开能力；换掉它的理由与公开性无关，见 [ADR 0013](adr/0013-claude-code-hooks-run-a-helper-not-a-port.md)。
- **Claude Code 行的内容预览同样不在本表记录**，尽管同一行的**标题**在表内。两者来源不同：标题只能靠解析私有 transcript JSONL 拿到，预览来自官方 Hook `MessageDisplay`——官方描述 "While assistant message text is displayed"，官方 payload `turn_id, message_id, index, final, delta`，走的是已经在用的 `type: "command"` handler。既没有私有 schema，也没有私有路径，因此按 [`AGENTS.md`](../AGENTS.md) §8.1「不得仅因未经 App Server 实现就登记」不构成登记项。
- **导航所用的进程祖先链调用**（`sysctl(KERN_PROC_PID)` 的 `e_ppid`、`proc_pidpath`）与**终端标签页聚焦所用的 Apple Events**都不构成登记理由：前者是公共 API，后者走的是 Terminal.app 与 iTerm2 **自己公开的脚本字典**，与 Claude Code 无关。上表登记的是**「祖先里有 `Claude.app` 就是桌面端托管」这个进程形态与那个 bundle identifier**。
- **终端会话已读所用的三个 BSD 调用**（`sysctl(KERN_PROC_PID)`、`devname_r`、`stat`）是公共 API，与 `NSWorkspace` 的激活通知同性质，本身不构成登记理由；上表登记的是**焦点上报这个观察到的 CLI 行为**，因为那条路径的覆盖面依赖它。
- 精确打开 Desktop thread 使用官方 `codex://threads/<thread-id>` deep link 和 macOS 公共 Launch Services，不在本表记录。
- 额度、今日 token、Thread 列表、标题、Turn 详情和状态校正使用公开 App Server 方法，不在本表记录。

## Desktop 更新后的排查顺序

1. 记录新的 Desktop short version、build 与内置 `codex --version`；只有重新验证了表中的私有依赖后才更新本文验证基线。
2. 先运行 `NotchlineTests`，根据失败测试定位到上表对应 feature。
3. 对私有依赖只做只读检查：确认文件、标识或可执行路径仍存在，再确认最小 schema；不得修改 `app.asar`、注入 Desktop IPC 或把缺失值伪装成成功。
4. 如果官方文档或公开 schema 新增了等价能力，优先迁移到官方方法，并从本表移除对应私有实现说明。
5. 如果 feature 暂时失效，必须 fail closed：Project 使用 `Project unavailable`；会话状态保留最后可信四态值，只有 App Server 无响应才使用全局 Disconnected；不得改用 cwd、标题、时间或窗口焦点猜测。

## 维护规则

- 新增任何依赖未公开或未承诺兼容的 Codex 实现细节的生产 feature 时，必须在同一个改动中新增或更新本表行。
- 不得仅因为实现没有使用 App Server 而登记；官方 Hooks、官方 deep link、公开 CLI/SDK 接口和 macOS 公共 API 都是允许的实现方式。
- 每行至少保留 feature 定义、官方公开支持接口的能力缺口、真实实现方法、失效信号和代码定位。
- 私有 schema 必须有 fixture 测试、缺失/损坏测试和明确的保守降级；不得把解析失败解释为空集合或 `Chats`。
