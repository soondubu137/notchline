# Codex in Notch

Codex in Notch 以低干扰方式汇总用户当前仍需要关注的 Codex Desktop 处理轮次，并提供返回原会话的入口。

## Language

**会话（Thread）**：
一个长期存在、可包含多个处理轮次的 Codex 对话容器。
_Avoid_: 任务、Session、单次请求

**可导航根会话**：
不隶属于其他会话，并且能在 Codex Desktop 中以相同身份直接打开的会话。
_Avoid_: 相关会话、所有本地会话、子智能体会话

**处理轮次（Turn）**：
一个会话中由一次用户提交发起、直到成功、失败或取消终态的处理周期。
_Avoid_: 会话、任务、运行

**实时监视会话**：
包含仍处于监视生命周期中的处理轮次的可导航根会话；它是展开列表的一行。
_Avoid_: 最近会话、历史会话、所有未归档会话

**监视生命周期**：
从用户提交输入开始；活动轮次始终持续，终态轮次在 Codex Desktop 已读、归档、删除或失去可导航性时结束。
_Avoid_: 执行生命周期、固定保留时间、最近 N 条

**当前状态快照**：
在应用启动、重连或校正时，由当前活动投影与未读终态共同形成的完整观察；它决定当前会话列表与状态。
_Avoid_: 历史事件回放、旧列表缓存、最近一次事件

**当前活动投影**：
随处理轮次开始、等待和结束边界持续更新，只包含此刻仍活动的精确会话与轮次身份；终态和历史对象不属于该集合。
_Avoid_: 历史事件、旧 Running 缓存、最近活动列表

**实时生命周期事件**：
在本次监视运行期间新发生、可对当前状态快照做增量更新的生命周期变化。
_Avoid_: 历史日志、持久化状态、完整快照

**历史生命周期事件**：
发生在本次监视运行之前的一次性生命周期记录；记录本身不能证明任何会话当前仍存在或处于某种状态。
_Avoid_: 待恢复状态、启动快照、历史 Stop 状态

**未读终态**：
处理轮次已进入终态，但对应会话仍被 Codex Desktop 标记为用户尚未查看。
_Avoid_: 最近完成、短暂终态、Notch 未读

**Project**：
Codex Desktop 中由用户创建和管理、可以包含一个或多个仓库的会话分组。
_Avoid_: 仓库、workspace、cwd、路径名

**Chats**：
Codex Desktop 中不属于任何 Project 的会话集合。
_Avoid_: 默认 Project、未知 Project

**会话标题**：
Codex Desktop 当前为会话显示的标题；尚无标题且不允许使用内容回退时称为 `Untitled`。
_Avoid_: 文件夹名、仓库名、Mock 标题

**处理时间**：
与 Codex Desktop 当前处理轮次显示一致的墙钟经过时间，包含等待人工处理和设备睡眠期间。
_Avoid_: 模型计算时间、活跃执行时间、会话年龄

**会话状态**：
实时监视会话当前处理轮次的产品状态：Input needed、Approval needed、Running、Unknown、Error、Cancelled 或 Completed。
_Avoid_: 集成状态、Idle、Disconnected

**汇总状态**：
由所有实时监视会话归并得到、显示在顶部的单一状态。
_Avoid_: 全局会话、连接状态

**未知（Unknown）**：
某一个实时监视会话的真实状态无法可靠判定，但整体集成仍可继续工作。
_Avoid_: Disconnected、Idle

**断开（Disconnected）**：
实时监视集成整体不再可靠，因而不能可信地产生会话集合或汇总状态。
_Avoid_: 单会话未知、额度不可用、预览不可用

**集成可用性**：
Codex in Notch 与当前 Codex Desktop 版本之间的实时观察和导航契约是否可用。
_Avoid_: 会话状态、额度状态

**当前内容预览**：
Codex 已向用户公开的 prompt、进度、错误或最终回答的短片段，不包括原始推理、工具细节或敏感执行内容。
_Avoid_: 智能体思考、raw reasoning、完整正文

**隐藏当前内容预览**：
禁止实时监视器展示任何当前内容片段，也禁止用用户输入填补缺失的会话标题。
_Avoid_: 只隐藏预览行、隐藏会话元数据

**主额度窗口**：
当前 Codex Desktop 账户标记为 primary、由单一额度圆环表达的 rate-limit window。
_Avoid_: token 余额、总额度、最紧张窗口
